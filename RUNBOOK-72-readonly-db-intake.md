# RUNBOOK-72 — 只读数据库接入：开工前我需要的东西

基线 `a211744`，**1289 tests passed**（本轮**没有代码变更**；RUNBOOK-71 的修复已在这个 commit 里，
你们在盒子上打开的那份 `RUNBOOK-71-strict-zero-call-response.md` 就是它）。

---

## 0. 这份文档是干什么的

Owner 定了下一章：**把只读数据库接进 MDC assistant**（先 UAT，以后换 prod）。
我要开始写代码了，但**我一个真实表名都不知道**。

过去五轮 MCP 接入的每一个缺陷，形状都一样：**我对你们的环境做了一个断言**——
先是名字（`hk1`/`hkl`）、然后是响应形状（JSON 被我当文本读）、最后是值的格式（`alert_time`）。
所以这次反过来：**先问，你们答，我再动键盘。**

**怎么读这份文档**：

| 章节 | 性质 |
| --- | --- |
| §1 | **不回答我无法开始**——三种答案对应三套完全不同的架构 |
| §2 §3 | 决定第一版能做什么、哪些数据碰不得 |
| §5 | 我的设计预案，**请你们反驳** |
| §6 | 请**不要**给我的东西 |
| §7 | 我不等你们也会先做的（所以这轮不会空转） |

只想先回一件事的话：**§1 的 Q1 + §2 第一步的表名清单**。这两样到手我就能开工。

---

## 1. 会改变架构的三个问题（请优先回答）

### Q1. 这个 skill 到底是什么形态 —— 谁在执行查询？

Owner 说"通过一个 skill 接入，skill 里有一个 UAT 只读 token"。**"token"这个词让我猜是 C**，
但猜是这五轮所有事故的来源，所以我不猜：

| | 形态 | 对我意味着什么 |
| --- | --- | --- |
| **A** | 一段脚本 / CLI，盒子上**任何进程**都能起（含我们的 webapp Python 进程） | webapp 直连，我写一层连接 + 闸门 |
| **B** | 只在 Codex agent 会话里能用，别的进程调不到 | **webapp 不能直连**。这一章就退化成和 MCP 同形：**我出查询计划、你们执行、结果回填**——产品是"半自动"的。我需要**提前**知道，否则会做出一个跑不起来的东西 |
| **C** | 一个 MCP server / HTTP 网关，带 bearer token | 走已有的 `webapp/mcp_client.py`，传输层基本不用新写，加一个 server 条目就行。**这是我最希望的答案** |

**回答方式**：直接把这个 skill 的目录结构和 `SKILL.md` 贴给我，
**把 token、主机名、库名换成 `<REDACTED>`**——我要的是形状，不是值。

### Q2. 引擎和驱动

1. 什么库、什么版本？（Oracle / PostgreSQL / MySQL / MSSQL / DB2）
2. 盒子的 python 里**有没有**驱动？请直接跑一下把结果贴回来：

```bash
python -c "import importlib;[print(m, bool(importlib.util.find_spec(m))) for m in ['oracledb','cx_Oracle','psycopg','psycopg2','pymysql','pyodbc','sqlite3']]"
```

3. 能不能 `pip install`？（air-gapped 我猜不能，但请明确说）

如果**没有驱动、也不能装**，那就只剩两条路：CLI 子进程（`sqlplus`/`psql`/`mysql`）或者 Q1-C 的网关。
**这一条直接决定我写什么代码**，不是细节。

### Q3. "只读"是账号只读，还是我们自律？

- 连接用的账号在**数据库层面**是不是只有 SELECT 权限？把授权贴一下（Oracle: `SELECT * FROM user_tab_privs`；PG: `\dp` 或 `has_table_privilege`）。
- 如果是 → 我的本地闸门是**第二道**保险。
- 如果不是（比如账号其实能写，只是大家约定不写）→ 我的闸门就是**唯一**一道，得写得严格得多，也会更容易误伤正常查询。**这两种情况我写出来的代码不一样。**
- 另外：我们的查询会不会被审计表 / 触发器记录？如果会，我要在产品文案里**如实说**"你的每次提问会在库里留痕"，而不是让人事后发现。

---

## 2. 有哪些表 —— 分两步给，别一次给全

### 第一步：只要**表名清单**（不要列结构）

按你们的引擎挑一条：

```sql
-- PostgreSQL
SELECT table_schema, table_name FROM information_schema.tables
WHERE table_type='BASE TABLE' AND table_schema NOT IN ('pg_catalog','information_schema')
ORDER BY 1,2;

-- Oracle
SELECT owner, table_name, num_rows FROM all_tables
WHERE owner NOT IN ('SYS','SYSTEM') ORDER BY 1,2;

-- MySQL
SELECT table_schema, table_name, table_rows FROM information_schema.tables
WHERE table_type='BASE TABLE' AND table_schema NOT IN ('mysql','sys','performance_schema')
ORDER BY 1,2;
```

**回答格式**（CSV 或 markdown 表都行）：

```
schema | table | 估计行数 | 一句话这是什么（能写就写，写不出留空）
```

**为什么要行数**：决定哪些表能整表读进模型上下文，哪些必须带条件——
这是查询预算的输入，不是好奇。

### 第二步：我圈 10–20 张，你们回列结构

每张表：**列名、类型、是否可空、主键、唯一约束、索引**。
唯一约束我会当**幂等的直接证据**用（`docs/8-showcase-readiness-and-data-requests-zh.md` 案例 3 卡在这里）。

**如果你们想直接给，这几张我最想先要**（前四张我现在用的是快照，最想变实时）：

- `tbl_use_case_router`
- channel rule 那张（真实表名我不知道）
- `tbl_event_router_usecase_topic`（你们 2026-07-30 确认 UAT 里存在）
- use case 主表
- 消息主表 / 状态表 / 回执表（案例 1、3 卡在这里）
- **加上你们认为"assistant 最该看"的那两三张**——这一条比我列的清单更有价值，你们比我懂。

---

## 3. 哪些列碰不得 —— 这条比表名更重要

请按表列出**含客户数据的列**：手机号、邮箱、姓名、账号、证件号、地址、**消息正文**。

两个用途，第二个才是重点：

1. 出口闸门 `webapp/redaction.py` 已经在跑（日志那条路），DB 这条路会**复用同一道，不新开路**。
   但它是**模式匹配，不认列名**——认得出手机号，认不出"这一列是客户姓名"。
2. 有了列名清单，我可以在**取数那一层就不 SELECT 它们**。事前不取比事后脱敏强一个数量级：
   没取出来的东西，不可能泄漏。**数据库行里有客户数据的概率比日志高得多。**

顺带问一句：**有没有已经建好的脱敏视图**，或者一个"根本看不见 PII 列"的只读账号？
如果有，我优先连那个，上面这份清单就降级成核对用。

---

## 4. 环境：UAT → prod 的时候，除了 token 还有什么会变？

主机、端口、schema 名、表名前缀、列名、时区、字符集——哪些会漂？

我要把"环境"做成**配置里的一个字段**，不是代码里的 `if prod:` 分支。
前提是我知道**哪些东西跨环境会漂**——不知道就只能全都硬编码在一边，换 prod 时逐个炸。

另外确认一句口径：UAT 的数据是**生产拷贝 / 脱敏拷贝 / 造出来的假数据**？
（`8-showcase` 里已经定过"UAT ≠ 生产"的表述纪律，这里只是确认底层事实。）

---

## 5. 接上之后怎么让 AI 用它 —— 我的预案，请你们反驳

### 默认方案：**不让模型写 SQL**

`config/db_queries.json`：一批**命名查询**。
**抽象名字我定，SQL 文本和真实表名列名你们维护**——和 `config/mcp_tools.json` 一模一样的缝。
模型只能做两件事：**选一个名字 + 传参数**（走绑定变量，不拼字符串）。

| | |
| --- | --- |
| **好处** | 注入面为零；表名改了你们改配置、不用等我推代码；每条查询的行数上限/超时/能不能进上下文可以**逐条**标 |
| **代价** | 模型答不了没预置的问题——但它会**明说"这个查询还没接通"**，而不是瞎编一个 |

这个代价我认为是对的：这一章的失败模式不是"答得少"，是**"拿 UAT 的数据自信地说生产的事"**。

### 5.1 接受这个吗？

还是你们更希望允许**受限 free-form SELECT**（白名单表 + 强制 LIMIT + 只读账号 + EXPLAIN 预算）？
如果是后者，请说清楚：**一条跑飞的查询打到 UAT 库上，谁负责**。

### 5.2 第一批命名查询做哪几条？—— 请在这张表上标注

**这张表的回答就是我第一版的功能清单。** 请标：能答 / 不能答 / 能答但要连表（顺便写涉及哪几张表）。

| # | 问题 | UAT 能答？ | 涉及哪几张表 |
| --- | --- | --- | --- |
| 1 | 某个 use case 现在路由到哪个渠道 / 哪个厂商？ | | |
| 2 | 某个 repo / 应用发出的消息走哪条 `delivery_path`？ | | |
| 3 | 某条 tracking ID 的消息现在什么状态？ | | |
| 4 | 某渠道最近 N 小时的成功 / 失败分布？ | | |
| 5 | 某个 use case 的 `rule_text` **当前值**是什么？ | | |
| 6 | topic ↔ use case 的映射（`tbl_event_router_usecase_topic`） | | |
| 7 | 某厂商停服 → 受影响的 use case 清单 | | |
| 8 | **快照 vs 实时的差异报告**：我手上的快照有多旧、哪几行漂了 | | |

**第 8 条我认为是第一版最该先做的**，理由：它不改变任何现有结论，只回答"我现在说的话有多旧"。
风险最低、对现有一整套快照结论的可信度提升最大——而且它天然是只读的、行数可控的。

### 5.3 结果能进模型上下文吗？

一次最多多少行 / 多少字符是你们能接受的？（我们已有 `webapp/context_budget.py`，给个数就行。）

### 5.4 谁能用？

所有 webapp 用户，还是要分档？我们已经有 `caller_policy` 那套分档（产品 / 内部 / 调试），可以直接套。

---

## 6. 请**不要**给我的

- **token / 密码 / 连接串**——只要**环境变量名**。（贴过来我也用不了：外部这边根本连不到你们的网。）
- **真实客户数据行**——样例请给"列名 + 类型 + **一个假的示例值**"。
- 任何你们不确定能不能外发的东西——不确定就先不给，我们照样能推进 §7。

我这边的承诺（五轮教训，见 RUNBOOK-61/62）：
**代码里不会写死任何一个你们的真实表名、列名、格式**。全部进 `config/db_*.json`，
你们能改、不用等我推代码——因为你们**推不了代码**，这一点是前提不是习惯。

---

## 7. 不等你们，我也会先做的

这样这一轮不会因为等回答而空转。等你们的名字到了，剩下的就是填配置：

1. **只读硬闸门**：语句类型白名单、禁止关键字、强制 `LIMIT`、语句超时、
   `SDLC_DB_MAX_QUERIES` 查询预算（照抄 `SDLC_INCIDENT_MAX_LOG_QUERIES` 那套）。
2. **出口复用** `webapp/redaction.sanitize_packet` —— 不新开一条"生产数据 → 浏览器"的路。
3. **没配置就 fail closed**：没有 config = 助手明说"数据库还没接通"，
   **绝不退化成"查不到 = 没有这条数据"**。（这正是 LogDream keyword 那个 P0 的形状。）
4. **一个 sqlite 假库跑全部测试**——你们的名字一个都不在测试里。

---

## 8. 最短路径

只回两样也行：**Q1（skill 的形态）** + **§2 第一步的表名清单**。
其余的可以边写边补。
