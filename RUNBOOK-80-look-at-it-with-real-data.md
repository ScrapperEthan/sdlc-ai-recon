# RUNBOOK-80 (INTERNAL Codex) — 把新版本跑起来,**用真实数据看一眼**

> 你们 RUNBOOK-79 的回报**全部达成**,而且 Q1 的答案是这一轮最有价值的发现:
> **322 个有证据的仓库里,127 个命中了至少 2 个渠道** —— v1 结构一直在把它们各自压成一个。
> 那是**将近四成的仓库被少报了渠道**,正好是我们花这么大力气在消除的那种少报。
> 你们直接升了 v2(663 条),而 reader 早就支持,**所以外网一行没改就生效了。**
>
> 引用复核 **663 ok / 0 stale / 0 unverifiable** —— 那 5 条 `.js`/`.groovy` 也转正了。
>
> 这一轮**不是再改数据**,是**第一次用真实数据把界面打开看一眼**。
> 预计 20–30 分钟。**这是发布前的最后一关**,因为到今天为止,那些卡片我只在占位数据上验过。

---

## 一、先决条件

```bash
git pull && python -m pytest tests -q -p no:cacheprovider
```

期望 **1507 passed**。

> 这次外网只改了两个小东西:
> 1. **`impact` 不带 inline 时也带上渠道分层** —— 否则模型只有在"顺便画了图"的那些轮次才看得见证据,
>    「这个仓库管哪些渠道」在没人点图的时候又会退回成一行逗号分隔的清单。
> 2. **提示词补了一条**:问"某个仓库管哪些渠道 / 改它影响哪些渠道"时也要展开影响视图。

## 二、启动

```bash
start.bat
```

打开 `http://127.0.0.1:8765`(只开这一个,另一个端口是内部的)。

---

## 三、要看的三种仓库(**这三种就是新工作会显形的地方**)

请分别问一句,然后**看渠道卡片**:

### ① 一个**多渠道**仓库(127 个里随便挑一个)

> 问:`<仓库名> 管哪些渠道?`

**要确认的**:
- 是不是**每个渠道一张卡**,而不是挤成一行
- 新增的那 341 条是 `low`,所以**第二个渠道应该带一个琥珀色「低置信」徽章** —— 这是对的,不是 bug
- 点开「证据 N 条」,**点引用能不能跳到源码那一行**

### ② 一个**共享库 / hub**(下游几百个的那种)

> 问:`改 <仓库名> 会影响哪些渠道?`

**要确认的**:
- 卡片有没有**把"它自己发的"和"会被连累的"分开**(实线卡 vs 虚线卡)
- 「间接受影响的渠道」是不是**默认折叠**的
- 最下面那两行覆盖度:
  - 「查过没发现 N 个;在本轮扫描范围外、且没有其他线索 M 个」
  - 「全库另有 55 个仓库扫过、读了源码和配置、没找到渠道标记 —— 这是一个结论,不是未知」
- **⚠️ 重点看措辞对不对**:范围外那个数是**条件子集**,不能读成"范围外总共这么多"

### ③ 一个**只靠名字**判断渠道的仓库(有渠道词、但没有代码证据)

> 问:`<仓库名> 是发短信的吗?你怎么知道的?`

**要确认的**:
- 徽章是不是**虚线空心的「名称推断」**,而不是实色
- 回答里有没有明说**"这是从仓库名推断的"** —— 如果它说得像查过代码,那是**提示词没按住模型**,请原样记下来

---

## 四、请回报:**三个真实的"改之前 / 改之后"例子**

这一条是业主直接要的 —— 他要拿去跟别人解释,而**只有你们能用真实名字举例**。

每个例子两行就够:

```
仓库:<真实仓库名>
问题:<你问的那句话>
改之前会答:<一行,通常就是几个渠道名>
改之后答:<渠道 + 直接/间接 + 凭什么 + 有没有引用>
```

三个例子建议就用上面 ①②③ 三种。**截图也行**,业主看得懂截图。

> 如果发现**哪一个的显示不对、或者读起来会让人误解**,那才是这一轮最值钱的回报 —— 请原样写下来。

---

## 五、顺手把评测用例的三个占位符填了(**你们现在正好有数据**)

`config/eval_repos.json` 里这三个是空的,空着它们的用例会**跳过**:

| 占位符 | 现在怎么挑 |
| --- | --- |
| `hub_repo` | 下游几百个的共享库 —— 就是上面 ② 你挑的那个 |
| `name_only_channel_repo` | 有渠道词但在 `CHANNEL_EVIDENCE.json` 里查不到证据的 —— 上面 ③ 那个 |
| `multi_evidence_repo` | **同一个渠道有多条依据**的(名字 + 代码都指向同一个渠道) |

⚠️ 注意 `multi_evidence_repo` 要的是**一个渠道多条依据**,不是"一个仓库多个渠道" ——
用例检查的是「四条依据的同一个渠道,必须报成**一个**渠道」。

填完可以跑:

```bash
python -m evals.run --case channel-*
```

四道题各自的判定标准写在 `evals/cases.jsonl` 每条的 `why` 里。
**红了第一嫌疑人是断言,不是模型** —— 这条规矩上次让我们少改错一次。

---

## 六、这一轮**不做**的事(讲清楚边界)

- **不是上线给全团队用。** 应用仍然只绑 `127.0.0.1`、没有登录、没有操作审计。
  给几个人试用没问题;**要给团队用,得先做那一块**,那是另一件事。
- **不动数据。** 证据文件、范围文件都已经好了,这轮只看显示。
- **11 → 7 个冲突**留给业主判,不在这轮。你们给的判断建议(高置信代码证据优先,
  但不自动覆盖业务表/名称,低置信不裁决)**已被采纳为口径**。

---

## 一句话

**跑起来,看三种仓库,给三个真实的"改前/改后"例子,顺手填三个占位符。**
显示不对的地方,原样写下来 —— 那比"一切正常"有用得多。
