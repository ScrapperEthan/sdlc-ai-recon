# RUNBOOK-82（内网 Codex）—— 知识层 + PROD candidate 的**答案行为**验收

> **前置**：RUNBOOK-81 已回报；知识层（字段词典 + 概念块 + `mdc_knowledge` 工具 + 提示词那一块）
> 已按 `docs/specs/mdc-knowledge-layer-zh.md` 落地；PROD candidate 已生成但**未 activate**。
>
> 你们设计文档 §14.2 / §15 已经有摄取侧的 canary 清单（SHA、行数、join 覆盖、manifest），
> **这份不重复那些**。这份只管一件事：**打开界面，用嘴问，看它说出来的话对不对。**
>
> 分成两组：**必须答得出**（12 题）和 **必须永远不说**（8 条红线）。
> **第二组比第一组重要** —— 我们这个项目所有的翻车都是第二组的失败：
> 服务端静默回文件尾部被我们标成"命中"、0% 被说成"关掉了"、JSON 被当文本读。
> 那几次的共同点都是**答案看起来很像对的**。
>
> 预计 60~90 分钟。

---

## 零、准备

```bash
git pull && python -m pytest tests -q -p no:cacheprovider
start.bat
```

canary 用环境变量指向 candidate，**不要改全局 active**：

```powershell
$env:SDLC_USECASE_DATASET="...\index\usecase-snapshots\prod\20260817-v1"
```

每题请回报：**问题 / 助手原话（关键两三句即可）/ 通过·不通过 / 不通过时它说错了什么**。

---

## 一、A 组：必须答得出（12 题）

### A1~A3 概念层（不该出现任何具体 use case 的配置）

| # | 问 | 通过标准 |
|---|---|---|
| A1 | `bounce-back 和 resend 有什么区别？` | 说清"没收到投递确认→转下一渠道" vs "系统失败后重发"，带文档小节定位；**不要**顺手报某个用例的配置 |
| A2 | `SLA 和 SLO 有什么区别？` | 业务期望值 vs 系统派生结果；说明派生值是只读的 |
| A3 | `MDC 是怎么运作的？` | 一条链路讲完（业务场景→上游请求→识别用例→渠道/模板/路由→投递状态/回退→治理与报表），**不是**一堆 `file:line` |

### A4~A6 字段层

| # | 问 | 通过标准 |
|---|---|---|
| A4 | `bounce_back 这个字段是什么意思？` | 语义 + 取值 + B/D/M/S + **这批快照里有没有这一列** |
| A5 | `message_owner 是谁维护的？` | 必须说出"文档定义了这个字段，但这批 PROD 导出没有承载它的表" —— 这是本轮**最关键的一题** |
| A6 | `use_case_type = M 是什么意思？` | 若 RUNBOOK-81 Q5 确认了码表：说出 Event Trigger + **标来源是 metadata 文档、非生产验证** |

### A7~A9 快照层（PROD candidate）

| # | 问 | 通过标准 |
|---|---|---|
| A7 | `<挑一个 PROD use_case_id> 现在走哪些渠道？` | 从 channel_rule 取（事实），provenance 显示 `environment=PROD` + 正确的 snapshot id |
| A8 | `这个 use case 的 rule_text 是什么？` | 明确 unavailable + 说明原因（这批导出没有 ext 表），**不许**去 UAT 拿 |
| A9 | `PEGA 出问题影响哪些 use case？` | 能答的部分照答，答不了的（endpoint→repo 链）明确说断在哪一跳 |

### A10~A12 两类结论必须**分开写**

| # | 问 | 通过标准 |
|---|---|---|
| A10 | `高风险消息为什么要双厂商？现在哪些用例配了？` | **两段**：前段标"文档说"，后段标"这批 PROD 导出里有 N 条"；两段各自带出处，中间不能糊在一起 |
| A11 | `声明了高风险但没开双厂商的有哪些？` | 出体检清单，措辞是"文档说 X，数据里 N 条不满足，请业主确认是文档过时还是配置漏了"，**不是**"违规/必须修复" |
| A12 | `厂商流量怎么分？` | 必须去快照取实际值；**绝不能**引用文档里的示例百分比（见红线 R1） |

---

## 二、B 组：红线 —— 出现任何一条即为不通过

| # | 红线 | 怎么试 |
|---|---|---|
| **R1** | 把文档里的示例百分比（HTCL 70% / CSL 30% 那类）说成当前配置 | 直接问 `HTCL 和 CSL 现在各分多少流量？` |
| **R2** | 把 TBC / 已停用（Confirmed demised）字段说成现行规则 | 挑一个 workbook 里标 TBC 的字段问它"这个字段现在怎么用" |
| **R3** | 用 UAT 的 ext 补 PROD 的答案 | A8 / A9，看有没有偷偷冒出 owner 或 rule_text |
| **R4** | PROD 数据集下出现 "UAT" 字样 | 任意一题，看 provenance 那一行（你们设计 §5.6 定位的两处硬编码） |
| **R5** | 因为 `production_verified=false` 就改口叫 UAT，或因为 `environment=PROD` 就说"生产验证过" | A7 的口径句 |
| **R6** | 把"文档里有定义"说成"当前有配置" | A5 |
| **R7** | 冲突时自己选边、合并成一个最终答案 | 找一个 workbook 与 `config/rule_text_semantics.json` 说法不同的字段问 |
| **R8** | **把已经业主定案的事重新报成"存在冲突"** | 问 `rule_text 和 priority 谁说了算？` —— 必须直接给业主 2026-07-27 的定案（rule_text 权威、priority 仅空时兜底），**不许**报成待确认 |

> R8 是新增的倒退风险：知识层上线后，workbook 会成为第四个声音。已定案的结论如果被重新翻出来当
> 悬案，在用户眼里是**功能倒退**，比不上线还糟。这就是 spec §10 里那张
> `config/knowledge_conflicts.json` 登记表要先存在的原因。

---

## 三、工具行为的两个小检查

1. `mdc_knowledge(query=..., field=...)` **两个参数都传** → 必须**报错**，不许静默取其一。
   （`usecase_routing` 上我们正好踩过这个：同时传 `use_case_id` 和 `topic` 会退化成配对查询，
   把兄弟用例全藏了。）
2. `mdc_knowledge` 的返回值里有没有那条 `next_for_current_config` 指针。
   **它必须在返回值里，不能只写在提示词里** —— 写进数据里的缝才真的生效，这是 MCP 那三轮的教训。

---

## 四、回报

```
A 组：12 题逐题 通过/不通过 + 不通过的原话
B 组：8 条红线 有没有被踩到（踩到请贴原话）
工具：两个小检查
另外：你们觉得哪几题的"通过标准"我定得不对 —— 前四轮的缺陷都是你们读出来的，这轮照旧
```

**不通过的题不要就地改提示词硬堵。** 先回报，我们看是提示词的问题、工具返回值的问题，
还是断言本身写错了 —— 我们的评测集出过一次"5 条红全是断言写错、模型其实是对的"，
所以**红了第一嫌疑人是断言，不是模型**。
