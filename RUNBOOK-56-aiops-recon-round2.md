# RUNBOOK-56 —— AIOps 侦察第二轮(9 个问题)

> **前提:** RUNBOOK-55 已完成,质量很高,结论已采纳。本轮**不重复问已回答的**,只问
> 第一轮**答案本身引出的新问题**,以及**能让外网侧立刻开工的最后几件事**。
>
> **第一轮之后判断的变化(先让你知道为什么问这些):**
> 1. `delivery_path` 匹配 0/8 ——**不影响主线**。CloudWatch 告警名 93.2% 内嵌完整仓库名,
>    而 `仓库 → topic → use case → 渠道/厂商` 这两跳**现在就有现成工具**
>    (`retriever/messages.py:36` + 反查 use case)。原来那条 Path 捷径断了,主路一直都在。
> 2. **Portal 404 不用管** —— 唯一依赖 Portal 的功能(重发判断)已经决定不做。计划一行不改。
> 3. **B4 的 36% 不需要补到 100%** —— 我们只需要**实际出现在告警里的那十几个 app**,
>    不需要把 98×460 全解出来。见问题 4。
>
> **谁做:** 问题 1、3、5、6、7 内网 Codex 跑;问题 2 需要看真实告警;问题 8 是问人的。
> **纪律同 RUNBOOK-55:** 只回报取值/计数/结构,不回报日志原文、告警里的客户标识、人名。
> **只读,不写盒子,不动 MCP 那边任何配置。**

> 📌 地址拼写:RUNBOOK-55 写作 `hk125254508`,截图我读作 `hkl25254508`。
> **以内网实测为准**,外网侧不硬编码任何地址(全部走环境变量)。这条不用回答。

---

## 问题 1 ⭐ —— `hk1` 和 `hkp3` 到底是什么?

第一轮发现 LogDream 有两个 source,而且**同一个 app 在两边都有**(`batchLetterPostman`)。

**要回报:**
1. 这两个是什么维度的区分?(生产/非生产?两个机房?两个 region?主备?)
2. **哪一个是生产?** 还是两个都是生产的不同机房?
3. `logdream_capabilities` 里对 source 有没有说明文字?有的话贴回来
4. 事故发生时,人**实际上会去查哪一个**?

> **为什么最先问:** 查错 source = **一条日志都查不到**,然后模型会自信地回答"未发现异常"。
> 这是这个系统里最容易犯、后果最严重的一类错误。在搞清楚之前,查询计划必须
> **两个都查并标注来源**,不能默认一个。

---

## 问题 2 ⭐⭐ —— 那 338 条告警,到底长什么样?

截图的告警分布里,**最大的一个家族是 `MDC Alert - General SHP API Error`,338 条,占 53%**。
但第一轮我们摸清的是 CloudWatch 的 ECS 告警格式(`prodECS_<repo>_service_...`),
**这 338 条完全是另一回事,我们对它一无所知。**

**要回报:**
1. 它是不是 CloudWatch 告警?**在第一轮那 500 条 `list_alarms` 里能不能找到?**
2. 如果不是 —— 它是谁发的?(MDC 应用自己发的邮件?Portal?别的监控?)
3. **告警原文长什么样**(客户标识 `<masked>`,只要结构和字段)
4. 里面有没有:时间、时区、**应用名或仓库名**、异常类名、API 名、错误码
5. "SHP" 是什么的缩写?(截图里还出现过 `AWS-SHP SMS OTP API ERROR`、`MDC Alert - General
   SHP API Error - mc-hk-hase-pfp-outbound-api` —— 最后这条**直接带了仓库名**,
   说明这个家族有时候是带仓库名的,要确认是不是普遍规律)

> **为什么这条最重要:** 覆盖 53% 告警的家族,我们连它从哪来都不知道。
> 如果它不是 CloudWatch 告警,那它大概率走 LogDream 那条路(Portal exception),
> 也就是说 **B4 的 app 映射问题会卡住一半以上的告警量** —— 这直接决定优先级。

---

## 问题 3 —— `delivery_path` 的 1–9,先 grep 镜像,别急着问业务方

第一轮结论:`delivery_path` 是数字枚举 `1,2,3,4,5,6,8,9`(**缺 7**)。

我把告警名里的文字 Path 重数了一遍,是 **9 条**(RUNBOOK-55 里我写成了 8 条,是我数错了):

```
1 WPB Servicing Realtime High Risk Path        6 WSB CMB Servicing Realtime General Path
2 WPB Time Critical Path                       7 WPB Servicing Batch Path
3 WSB CMB Servicing Realtime High Risk Path    8 WPB Servicing Realtime Standard Path
4 WPB Servicing Realtime General Path          9 WPB Marketing Batch Path
5 WPB Servicing Realtime Express Path
```

**9 个文字名 vs 8 个出现的数字 + 缺掉的 7 = 9 个槽位。** 强烈提示这就是同一个 1–9 枚举,
7 只是在 UAT 快照里没有用例而已。(上面的编号是我瞎排的,**不要当成答案**。)

**要回报**(按顺序做,做到哪条有结果就停):
1. 在 460 仓库镜像里**全文搜这 9 个字符串**(如 `"WPB Time Critical Path"`)——命中哪些文件?
2. 搜 `deliveryPath` / `DeliveryPath` / `DELIVERY_PATH` 的**枚举或常量定义**
3. UAT 库里有没有一张 `delivery_path` 的**码表/字典表**
4. 以上都没有 → 才去问业务方要 1–9 的对照

> **为什么这么排:** 问业务方要一次往返好几天。如果这个映射就写在我们自己的代码里,
> 那是几分钟的事。**能自己查到的不要去问人。**

---

## 问题 4 ⭐ —— 只要 top 告警涉及的那十几个 app,不要全解

第一轮:规则唯一匹配约 36%,部署脚本证据有大量歧义。**不用补到 100%。**

**要回报:**
1. 把告警清单里**出现次数最多的前 10 个告警家族**,各自对应的 **app 名 / 仓库名**列出来
   (从告警名里能读出来的直接读;读不出来的标"未知")
2. 这些 app **去重后一共几个**?
3. 这几个里,**规则命中 + 部署脚本证据两个信号是否一致**?
   - 两个都指向同一个仓库 → `confirmed`
   - 只有一个信号 → `candidate`
   - 两个冲突,或都没有 → `unknown`
4. 三档各几个?`unknown` 的是哪几个?

> **判断标准:** 如果去重后只有 15~25 个 app,那这件事就不是"建一套映射系统",
> 而是"人工确认一张二十行的表"。剩下 70 多个 app 等真的出现在告警里再补。
> 三档置信度的做法这个项目用过(RUNBOOK-46 的 `via` 字段),`unknown` 一律 fail closed,
> 助手要明说"这个 app 我对不上仓库",绝不猜。
>
> 表建好后放 `config/logdream_app_map.json`,按 `AGENTS.md` 归内网 Codex 维护。

---

## 问题 5 ⭐ —— 三个读日志工具的完整参数

这三个是查询计划最终要填的表格,**参数字段名和格式必须一字不差**:

- `search_logdream_log_files`
- `read_logdream_log`
- `investigate_logdream`

**要回报:** 每个工具的**完整参数清单**(名字 + 类型 + 必填/选填 + 默认值),重点确认:
1. 时间参数**叫什么、什么格式**(epoch?ISO8601?带不带时区?)
2. **有没有时区参数?** 第一轮说"LogDream 读取默认时区为 Asia/Hong_Kong" —— 这个默认能不能显式覆盖?
3. `alert_time_backtrack` 模式的参数怎么传
4. 返回内容**有没有大小/行数上限**?能不能限制?(决定我们要不要自己截断)
5. `investigate_logdream` 的输出结构(它是"综合排障接口",返回的是结构化 findings 还是一段文本?)

> 这一条给完,外网侧就能把查询计划的数据结构定死并写单测。

---

## 问题 6 —— `list_alarms` 的全量和分页

第一轮:`maxResults=500` 返回正好 500 条(疑似截断),耗时 26.42 秒。

**要回报:**
1. **告警总共多少条?**(有 `nextToken` 之类的分页参数就翻完)
2. 翻全量要多久?
3. 500 条以外的那些,内嵌仓库名的比例还是不是 93%?(抽查即可)

> 为什么问:26 秒的调用**不能放进事故排查的实时路径**。
> 如果全量很大,那就得预先拉一份缓存下来,而不是每次现拉。

---

## 问题 7 ⭐ —— 事故样本:不用等团队给,自己造

第一轮:E14 现有样本 0 个。**这是现在外网侧最硬的一堵墙** —— 我碰不到生产,
没有样本就写不出也测不了查询计划。

但 CloudWatch 那 30 个工具**本身就是只读的历史查询**,可以自己造样本。

**要做的:** 挑 **3 个已经恢复的真实告警**(最好覆盖不同类型:一个 ECS CPU/内存、
一个 RDS/Aurora、一个 SQS 或别的),对每个跑一遍:

```
parse_cloudwatch_alert → get_alarm_history → get_metric_window
                       → get_recent_cloudtrail_events → 对应资源的 diagnostics
```

**把每一步的「请求参数 + 返回内容」原样存成 JSON**,然后:

1. **脱敏**:客户数据、账号、人名、内网主机名、ARN 里的 account id 一律替换成 `<masked>`
2. 存成 3 个文件,文件名用 `case-01.json` 这类,**不要用真实事故编号**
3. 回报:**脱敏后这几个文件能不能带出内网**(这是合规问题,要先问清楚再动)

> **为什么这条价值最高:** 有了这 3 个文件,外网侧就能把整条链路写完、写测试、跑绿,
> 完全不需要连生产。**没有它,外部这边只能空转。**
> 另外,截图里那个 Aurora 案例(`FailoverDBCluster` → 人工 roleswap → 连接池 stale connection)
> 就是理想样本,如果那个告警还查得到历史,优先用它。

---

## 问题 8 ⭐ —— A2 / A3 现在是头号阻塞,要去问人

第一轮全部"待确认"。技术上三个端点两个通,**但没人点头就不能接**。

**要回报:**
1. 这三套 MCP 的负责团队
2. 我们的助手调用它们:需不需要申请?有没有配额?
3. 对方介不介意我们**把他们的工具包一层**给我们自己的用户用
4. 现在这套 AIOps 是谁在用、怎么触发的?**同一个告警,今天的人工流程是几步、多久、卡在哪?**
5. 顺带把 E13 那 9 份 SKILL.md 的原文要过来(第一轮在镜像里 0/9,镜像版本停在 2026-04~05,
   说明它们不在我们镜像的仓库里)

> 这条是纯沟通,不需要跑任何命令,**但它现在比任何技术问题都靠前**。

---

## 问题 9 —— 顺带:盒子的 `/home` 满了

第一轮旁注:`/tmp` 21%、`/opt` 90%、**`/home` 100%**。

**要回报:** 这个是不是已知情况?会不会影响我们的服务?需不需要清理?

> 不是我们这条线的事,但 100% 会以很难查的方式咬人(写文件静默失败)。看到了就说一声。

---

## 回报格式

```text
1 source   : hk1=__ hkp3=__ ;生产=__ ;capabilities说明=__ ;人实际查=__
2 SHP告警  : 是CloudWatch=是/否 ;在500条里=在/不在 ;发出方=__ ;
             原文字段=[...] ;带仓库名=普遍/偶尔/没有 ;SHP=__的缩写
3 path枚举 : 镜像搜9个字符串=命中__处,文件=[...] ;enum定义=有/无,位置=__ ;
             码表=有/无 ;结论=能自己解决/需要问业务方
4 app映射  : top10告警家族→app/仓库=[...] ;去重后__个 ;
             confirmed__ / candidate__ / unknown__ ;unknown=[...]
5 工具参数 : search_logdream_log_files(...) / read_logdream_log(...) /
             investigate_logdream(...) ;时间格式=__ ;时区参数=有/无 ;
             返回上限=__ ;investigate输出=结构化/文本
6 告警全量 : 总数=__ ;分页参数=__ ;全量耗时=__s ;500条外内嵌率=__%
7 样本     : 造了__个 ;覆盖类型=[...] ;脱敏后能带出内网=能/不能/待批
8 权限     : 负责团队=__ ;需申请=__ ;对方态度=__ ;
             现有人工流程=__步/__分钟/卡点=__ ;SKILL原文=拿到__/9
9 磁盘     : /home 100% 是否已知=__ ;是否影响=__
```

**不要回报:** 日志原文、告警里的客户标识、`owner` tag 的人名、ARN 里的 account id、
完整数据行。

---

## 拿到答案之后,外网侧立刻能做的(不用再等)

即使这一轮还没回来,下面这几件已经可以开始 —— 第一轮已经给够了:

1. **CloudWatch 告警名解析器** —— `prodECS_<repo>_service_<Metric><Sev>[threshold]` → 仓库 id。
   93.2% 命中率已实测。纯离线单测。
   ⚠️ 注意 `index/*.json` 在 `.gitignore` 里(外网没有仓库清单),所以解析器**必须支持注入仓库
   清单**,不能假设本地有 —— 校验在盒子上做。
2. **MCP 客户端** —— 两种 transport 都已确认(LogDream=旧式 SSE,CloudWatch=Streamable HTTP),
   照着写 + 白名单 + 超时。
3. **查询计划的数据结构** —— 带**显式时区**字段。
   第一轮实测三个时区并存:**CloudWatch=UTC / LogDream=Asia/Hong_Kong / 服务器=GMT**。
   计划里绝不允许出现不带时区的时间。
4. **日志文件名进配置** —— 实测是 `otx_trace.log` 而不是同事 SKILL 里写的 `trace.log`,
   另有 `exception.log` / `sftp.log` / `*_YYYYMMDD` 历史文件。**这类字符串一律进
   `config/`,不进代码** —— RUNBOOK-49/50/51 已经证明过硬编码词表的代价。
5. **影响面主路** —— 告警 → 仓库 → `message_flow(repo=)` → topic →
   `usecase_routing(reverse=true, topic=)` → use case → 渠道/厂商 → 该通知谁。
   **这条链上每一步的工具都已经存在**,只差第 1 项那个解析器。
