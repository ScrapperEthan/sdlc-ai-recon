# RUNBOOK-84 —— 从 DeepSeek Harness 借七件东西：先确认六件事，再写 plan

> **这一份不改任何代码，也还不是 plan。**
> 2026-08-14 DeepSeek 开源了它的 agent harness（`deepseek-ai/deepseek-harness`，MIT，
> v0.1 developer preview）。外网把 50 个包对着我们的 `webapp/agent.py` 逐条比过，
> 挑出**七件我们确实没有、而且对 MDC assistant 有用**的东西（附录 A 给了全部设计要点，
> 内网上不了 github，不需要也不要去找原仓库）。
>
> 但这七件里，**至少两件可能应该直接砍掉** —— 砍不砍取决于内网机器上的实测数据，
> 外网猜不出来。这份文档就是去拿那些数据。
>
> **顺序**：这份 RUNBOOK 回报 → 外网出 plan（范围/阶段/开关/砍掉哪几条）
> → 内网确认 plan → 才写 implement md。**现在不要开始实现。**

---

## 0. 三条规矩（和 RUNBOOK-83 同一套）

1. **对不上先回报，不要顺手适配。** 外网已经因为"对着想象写"返工过五轮，
   每一轮的缺陷都是同一个形状：**我们对你们环境里的某样东西下了断言**
   （名字 → 响应形状 → 值格式）。这一份里凡是标了「外网快照里是这样」的，
   都可能已经变了，**以你们真机为准**。
2. **原样回传，不要总结、不要改写。** 尤其是字段名、路径、报错原文和数字。
3. 🔴 **不要把原始生产日志、未脱敏 payload、真实 alarm name、真实仓库名贴进回报。**
   下面每个探针要的都是**结构、计数、分类结果、是/否**。
   举例用占位符（`<app>` / `<repo>` / `<tool>`）或工具自己给的指纹。

**另外三条，这一份特有：**

4. **不要装任何依赖。** 上游是 TypeScript + pnpm + 50 包 monorepo，
   我们**一行代码都不抄**，只抄设计。附录 A 已经把设计写全了。
5. **不要为了回答这些问题去打生产。** 所有探针都能用只读查询、
   `LLM_MOCK=1`、或者已有的会话/检查点落盘数据回答。
   唯一的例外是探针 3b（provider 严格性），它需要一次真 provider 调用 —— 那是模型调用，不是生产系统调用。
6. **探针 1 如果 RUNBOOK-83 已经跑过，直接复用它的输出，不要重跑。**
   这里只补 83 没问到的三个点。

---

## 探针 1 —— 🔴 挂载点：两个模式共用什么？（先跑这个，它决定其余六条的成本）

外网手里的快照只有 **ask 模式**的循环（`webapp/agent.py:130 answer_events`）。
**agent 模式的循环是你们自建的，外网从没见过**（RUNBOOK-83 探针 1 已经在问它的文件清单）。

七件借鉴里有五件要挂在「工具派发」或「上下文预算」这两个点上。
如果两个模式共用这两个点，一次改动两边都生效；如果各走各的，
每一条的工作量翻倍，而且 compaction 那条可能对 agent 模式根本不适用。

### 1a 回报这三个（一句话一项）

| 问题 | 外网快照里 ask 模式的情况 | agent 模式呢？ |
| --- | --- | --- |
| 工具调用是不是都过 `tools.dispatch()` / `tools.dispatch_events()` | 是，`webapp/agent.py` 里只有这两个入口 | ? |
| 上下文控制是不是用 `context_budget.Budget()`（`reserve_system` / `fit_history` / `fit_tool_result`） | 是 | ?（不用的话，它靠什么控上下文？一句话） |
| 一轮里最多几次模型调用 | `MAX_TOOL_ITERS = 8` | ?（有没有等价的上限，叫什么） |

### 1b 再回报一个（这个直接决定 spill 要不要从头做）

`webapp_data/agent_turns/` 那些检查点里，**存不存工具返回的原始结果全文**？

- 存 → spill 有一半是现成的（已经有落盘的全文和一个可寻址的位置），plan 里这条会变得很便宜；
- 只存摘要 → 按附录 A.1 从头做。

回报形式：一个检查点文件的**键结构**（用探针 3 那种 `shape()` 手法，只要键名和类型，不要值）。

### 1c 期望

外网**没有预期**，这三项都是真问题。**别按外网的猜测填。**

---

## 探针 2 —— spill 的四个前提（七件里最值得做的一件，也是限制最多的一件）

**要做的事**（附录 A.1）：工具结果超过阈值时，全文落盘，
模型看到的换成「头尾预览 + 一个可回读的地址 + 检索提示」，而不是像现在这样切掉就永久没了。

外网已经确认的一个硬约束：`retriever/code.py` 里 `read_file` / `search_code`
**都经过 `_resolve_inside_mirror()` 硬绑在 `config.MIRROR` 里**
（`os.path.realpath` + 前缀检查）。所以模型**读不到 mirror 以外的任何路径** ——
这是一道正确的边界，**plan 不会去改它**。spill 要能回读，只有两条路，二选一是 plan 要定的事，
但前提得你们答。

### 2a 可回读性

| 问题 | 回报 |
| --- | --- |
| 允不允许**新增两个模型可见工具**（`read_spill(locator, offset, limit)` / `grep_spill(locator, pattern)`），根目录只限 spill 目录 | 允许 / 不允许 / 有条件 |
| 模型可见工具的数量现在有没有上限或预算（当年 21→13 那次合并的口径还在吗） | 现在几个、有没有硬上限 |
| 如果不允许新增工具：`read_file` 加**第二个允许根**可不可以接受 | 可以 / 不可以 |

### 2b 落盘许可与位置

| 问题 | 回报 |
| --- | --- |
| 银行机器上，把**工具输出**写到本地磁盘，合规上允不允许（`incident_raw_store` 已经在做类似的事，但它挂在 `SDLC_INCIDENT_RAW_LOGS` 这个 UAT 开关后面） | 允许 / 只在 UAT / 不允许 |
| 有没有留存或清理要求（审计要留多久？必须多久内删？） | 一句话 |
| `webapp_data/` 是不是可写、跨轮存活、跨重启存活 | 三个是/否 |
| webapp 是**单进程**还是多 worker / 多实例 | 单 / 多（多的话，同一个 owner 的下一轮会不会落到另一个进程） |

### 2c 🔴 脱敏边界（这条答错了就是给出口闸门开后门）

`incident_investigate` 那条链上有一道**按值整包指纹化**的出口闸门（RUNBOOK-64 加的），
原始日志文本不许离开进程。spill 如果挂在派发之后、闸门之前，
就会把**未脱敏原文写到磁盘**。

| 问题 | 回报 |
| --- | --- |
| 出口脱敏闸门是哪个函数、在哪个文件 | 函数名 + 文件名 |
| 它相对 `tools.dispatch_events()` 的返回是**之前**还是**之后**跑 | 之前 / 之后 |
| `redaction.py` 里那道 PII 闸门呢，同样的问题 | 之前 / 之后 |
| 除了 `incident_investigate`，还有哪些工具的结果**在派发出口时仍然含敏感原文** | 工具名列表（没有就写"没有"） |

**外网的立场（不用你们同意，但要你们确认能实现）：spill 必须挂在所有脱敏闸门之后，
写盘的是模型本来就能看到的那份文本。** 如果你们的闸门在别的位置导致做不到，
**回报，先别改任何东西** —— 那意味着这条要重新设计或者砍掉。

### 2d Windows

上游的落盘用的是「私有 0700 目录 + 随机文件名 + 独占创建 `open(path,'wx',0o600)`」，
目的是防同机器上别人读走、防符号链接劫持。我们踩过 `os.replace` 在 Windows 上偶发失败
（后来做了 `webapp/atomic_json.py`）。

```python
# 只读探测：在 webapp_data 下试一次独占创建 + 一次重复创建，看行为
import os, tempfile
d = tempfile.mkdtemp(prefix="spill_probe_", dir="webapp_data")
p = os.path.join(d, "probe.txt")
with open(p, "x", encoding="utf-8") as f: f.write("x" * 1024)
try:
    open(p, "x")            # 期望：FileExistsError
    print("SECOND OPEN SUCCEEDED  <-- 回报这一行")
except FileExistsError:
    print("exclusive-create OK")
print("mode:", oct(os.stat(p).st_mode), "size:", os.path.getsize(p))
os.remove(p); os.rmdir(d)
```

回报：这段的完整输出。另外一句话：**这台机器上，`webapp_data/` 的目录权限别人（同机其他账号）能不能读。**

---

## 探针 3 —— compaction：那次额外的模型调用付得起吗，以及它现在到底掉不掉

**要做的事**（附录 A.2）：现在 `fit_history` 放不下就**丢**历史轮次
（丢完给模型留一句"有 N 轮被丢了"）。上游是**先剪掉旧的工具结果**（不花模型调用），
不够再**总结**（花一次模型调用）。`config.py:129` 那条注释说 compaction lane 预留了
5% 但"要多花一次模型调用所以推迟" —— 这次就是来算这笔账的。

### 3a 🔴 它现在到底掉不掉？（这条可能直接让整件事作废）

`Budget.report()` 会给出 dropped 计数。从**真实会话**里统计（不要造数据）：

```python
# 按你们的 session store 实际路径改；要的是分布，不是内容
import json, collections
# 对每个已存会话的每一轮，取当时的 budget report（如果没落盘，见下面 3a-备选）
hist = collections.Counter()
# hist[dropped_轮数] += 1
print(json.dumps(dict(hist), ensure_ascii=False))
```

| 要什么 | 形式 |
| --- | --- |
| 真实会话里，一轮 `dropped > 0` 的**比例** | `x / y 轮` |
| dropped 的分布 | `{0: n0, 1: n1, 2: n2, ...}` |
| 最长的那次会话有几轮 | 一个数 |
| 有没有用户因为"上下文被丢了"实际抱怨过 / 有没有见过模型说"这个之前说过但我看不到了" | 一句话，有例子最好（脱敏） |

**如果 `dropped > 0` 的比例很低（比如 < 5%），这条在 plan 里会被降到最低优先级或者直接砍掉。**
外网希望它被砍 —— 那说明现有的 `HISTORY_MAX_ROUNDS=10` + lane 划分已经够了。

*3a-备选*：如果 budget report 没有落盘，就退一步统计
**每个会话的轮数分布**和**每轮 messages 的估算 token 总量**，同样能判断。

### 3b provider 严格性（决定"剪枝"能不能安全做）

剪掉/替换一条 `tool_result` 而保留它的 `tool_call`，多数 provider 会直接 400。
上游为此专门做了成对性检查。

| 问题 | 回报 |
| --- | --- |
| 真实在用的 provider 有哪几个（`llm_providers/` 里哪些真的被路由到） | 名字列表 |
| 每个 provider：把一条 `tool_call` 对应的 `tool_result` 的 **content 换成一段短文本**（不是删掉），还能不能正常调通 | provider → OK / 报错原文 |
| 每个 provider：**整条删掉** `tool_result` 而保留 `tool_call`，报什么错 | provider → 错误原文（原样） |

这一步需要一次真 provider 调用。**用最小的请求**，不要打生产系统。

### 3c 成本

| 问题 | 回报 |
| --- | --- |
| 一次典型排障 turn 现在打几次模型、大约多少 token | 两个数（`llm_usage` 应该有） |
| 模型调用有没有配额上限（Copilot token / 每人每天 / 每月） | 有的话，上限是多少，现在用掉多少 |
| 多花一次「总结」调用（估算 2–4k token 输入、几百输出），可接受吗 | 可 / 不可 / 只在某些条件下可 |

---

## 探针 4 —— `ask_user_question`：前端能不能在一轮中间停下来等人

**要做的事**（附录 A.4）：把"我需要问人"从 prompt 里的一句话，变成一个有 schema 的真工具。
这条直接对着我们被抓最多的那类错：**不知道的时候模型会编一个，而不是停下来问**
（示例 JSON 里编值、给字段安含义、`hkl` 读成 `hk1`）。

**分岔口**：阻塞式（turn 中间挂起等人回答，然后继续同一个 turn）比
"turn 末提问、下一轮再答"贵得多。选哪个由你们的前端/会话层能力决定。

### 4a 回报

| 问题 | 回报 |
| --- | --- |
| 现在的 SSE 事件流（`token` / `tool_start` / `tool_end` / `view` / `done`）能不能加一个 `ask` 事件，前端渲染成选项卡片 | 能 / 不能 / 要改多少 |
| 一个 turn 能不能**挂起**等待用户输入，然后在**同一个** turn 里继续（不是新开一轮） | 能 / 不能 |
| 如果不能：turn 在 `done` 时带一个结构化的 `pending_question`，下一轮用户的回答自动绑回去 —— 这个能做吗 | 能 / 不能 |
| agent 模式：计划执行到一半停下来等人，检查点和重规划受不受影响 | 一句话 |
| per-browser 的 `owner` 隔离现在是不是已经够（问题只对发起的那个 browser 可见） | 是 / 否 |

### 4b 顺带回报一个现状

现在 prompt 里那条"先给假设再澄清"，**实际生效吗**？
从 eval 集（22 条）或真实会话里找：**模型遇到不知道的枚举值/字段含义时，
实际是问了，还是编了。** 给个粗略比例和一两个（脱敏的）例子。

**这条如果显示模型其实已经会老实说"不知道"，这件事的优先级要降。**

---

## 探针 5 —— 重复调用打断器 ＋ 每工具超时（两个小东西，一起测）

### 5a 现在到底有没有循环？（没有就别加）

从真实会话的 `tool_trace` 里统计：**一个 turn 内，同名 ＋ 参数完全相同的连续调用，最长连跑几次。**

```python
# tool_trace 是 [{"tool": ..., "args": {...}}, ...]，按 turn 分组
import json, collections
def canon(a): return json.dumps(a or {}, sort_keys=True, ensure_ascii=False)
runs = collections.Counter()
# 对每个 turn: 扫 trace，数最长的 (tool, canon(args)) 连续段，runs[长度] += 1
print(json.dumps(dict(runs), ensure_ascii=False))
```

回报：`runs` 的分布 ＋ 出现过 ≥3 的 turn 占比 ＋ **最长那次是哪个工具**（工具名可以给，参数不要）。

### 5b 合法的重复有哪些（决定阈值和排除名单）

| 问题 | 回报 |
| --- | --- |
| `MCP_RETRY_ATTEMPTS=2` 的重试是在 `mcp_client` **内部**（模型看不见）还是模型自己重发 | 内部 / 模型 |
| MCP 闸门 fail-closed 拒掉一个调用后，模型实际会不会**原样再发一次** | 会 / 不会 / 没观察到（有例子最好） |
| agent 模式的**重规划**会不会合法地重跑同一个任务 ＋ 同一参数 | 会 / 不会 |
| 有没有哪个工具，同参数连调多次是正常的 | 工具名列表 |

### 5c 每工具超时

我们现在 `MCP_TIMEOUT = 60` 是一个全局值。日志全文搜索和读一个指标不该是同一个预算。
你们已经有 MCP 徽章带 latency，数据是现成的。

| 要什么 | 形式 |
| --- | --- |
| 各工具的实测 p50 / p95 延迟 | `{"<tool>": [p50, p95], ...}`，工具名可以给真名 |
| `tools.dispatch()` 是不是**同步阻塞**调用 | 是 / 否 |
| 现在有没有现成的超时/线程机制可以复用（除了 `mcp_client` 自己那个和 provider 的 HTTP timeout） | 有的话叫什么 |
| 哪些工具**实际上会慢**（>10s） | 工具名列表 |

**如果只有 MCP 和 `db_query` 会慢**，plan 里这条会缩成"给这两个各加一个覆盖值"，
不做通用的 per-tool 超时框架。外网倾向于这个结果。

---

## 探针 6 —— 两条纪律：愿不愿意接（这条不要数据，要一个态度）

代码现在整体归内网，所以这两条是加在**你们的**开发流程上，外网无权替你们决定。

**6a「模型眼里长什么样」文档段**（附录 A.5）：每个模型可见的工具/闸门，
文档里固定写三小节 —— 模型看到的**原文**是什么 / token 影响 / 变了会不会打破 KV cache 前缀。
理由：我们 90% 的缺陷是"模型看到的东西 ≠ 我以为它看到的"（响应形状、字段含义、
占位符、`alert_time` 格式），而这五轮全靠你们在真机上帮我们抓。

**6b「每个模块交一条运行时不变量，或写明为什么没有」**（附录 A.6）：
要么注册一条真能跑的检查，要么写一句 `No runtime invariant:` 加**针对这个模块的**理由；
一个 `verify` 脚本机械地拒掉空壳和敷衍。理由：这是把"不许沉默地跳过"制度化 ——
`citations 缺 .js/.groovy → 引用守卫被整个跳过` 就是典型的沉默空壳。

| 问题 | 回报 |
| --- | --- |
| 除了本地 `pytest`，有没有 CI（能自动跑门禁脚本的东西） | 有/没有，有的话是什么 |
| 现在每个模块有没有独立文档 | 有几个模块有，放在哪 |
| 6a 接不接 | 接 / 部分接（说哪部分）/ 不接 |
| 6b 接不接 | 接 / 部分接 / 不接 |

**不接是完全可以的答案。** 不接就从 plan 里删掉，不会影响其余五条。

---

## 回报格式

一个文件，六节，节标题就用 `探针 1` … `探针 6`。
每节先贴命令输出（**原样**），再回答那几个问题。
**没跑的探针写"没跑"和原因，不要留空。**

### 回报后外网出什么

一份 plan（不是 implement md），内容固定是这四块：

1. **砍掉哪几条、为什么** —— 外网现在的预判是**至少两条会被砍**
   （探针 3a 如果显示历史基本不掉 → compaction 砍；
   探针 5a 如果显示基本没循环 → 打断器降级或砍；
   探针 5c 如果只有两个工具慢 → per-tool 超时缩成两个覆盖值）。
   **这份 RUNBOOK 一半的价值就是让某几条被砍掉。**
2. **留下来的每条**：挂在哪个函数、加几个开关（开关默认全关）、
   影响哪些现有测试、以及**它防住的是哪一类已经发生过的缺陷**。
3. **阶段划分**：哪些可以独立上、哪些有前后依赖。
4. **每条的"不做什么"** —— 明确的范围边界，防止 implement 时膨胀。

plan 确认没问题之后，才写 implement md。

---

## 附录 A —— 借鉴对象的设计要点（内网上不了 github，这里给全，不需要去找原仓库）

上游：`deepseek-ai/deepseek-harness`，MIT，v0.1 developer preview，
外网已核对到 master 分支（2026-08-17）。**我们只抄设计，不抄代码。**

### A.1 spill（`packages/spill/*`，三个包）

三段式：**服务定义**（存什么、返回什么）/ **后端**（本地文件系统）/ **策略**（何时触发）。

- 触发：只对**最终的、纯文本的**工具结果生效，超过 `maxInlineBytes` 才触发。
  嵌套调用、`read` 自己的结果、被拒绝的调用、含非文本块的结果，**一律不碰**。
  排除 `read` 是为了避免 `read → spill → 再 read` 的死循环。
- 替换文本：头尾预览 ＋ 一行提示，**整体仍然不超过 `maxInlineBytes`**
  （提示的字节数先从预算里扣掉，预览再缩到剩下的空间）。提示原文形状：
  `（省略了 N 字节。完整结果存在 <地址>。用 read 加 offset/limit 读，或 grep 这个路径搜。）`
- **尽力而为**：没有后端、存储失败、没有会话归属 —— 一律记一条 warning，
  **返回原结果**。spill 失败绝不把一次成功的调用变成错误，也绝不隐藏原结果。
- 落盘：私有 0700 目录 / 按会话 id 的 sha256 前缀分组 / 文件名 = 随机十六进制 ＋
  清洗过的建议名（防路径穿越）/ 独占创建 `'wx', 0o600`（路径已存在就失败，
  不管它是不是符号链接 —— 防劫持）。
- 明确**不管**的三件事：保留期、访问控制、检索 API。归属只是命名空间，不是权限。

### A.2 compaction（`packages/compaction/*`）

- **两档触发**：`pressure`（压力）和 `context-overflow`（确认溢出），后者可以更激进。
- **顺序**：先跑**工具结果剪枝器**（不花模型调用），重新量一次，
  够了就**根本不总结**；不够才选区间去总结。
- **剪枝不许拆散 `tool_call` / `tool_result` 配对** —— 上游为此有专门的成对性检查函数。
- **总结落地方式**：不是删掉旧消息，而是新写一条消息，带一个"替换 [start, end] 区间"的标记，
  把旧区间**盖住**。旧的仍在日志里，可复盘。
- 🔴 **加锁：先写 `start`，做完再写 `end`。** 中途崩了会留下"有 start 没 end"的孤儿锁，
  **能被发现**；反过来先写 end 就变成"谎称压缩完成了"。
  —— 这跟我们那条教训完全同形状：没写台账就没法复盘，只好把猜测当结论。
- 失败分类是明确的枚举：`busy` / `cancelled` / `changed` / `summary` / `commit` / `persistence`，
  其中 `changed` 和 `summary` **不改变对话内容**但仍然把失败的这次记进日志。

### A.3 重复调用打断器（`packages/guard/repeat-tool-reminder`）

- **不进工具列表、不否决、不改写** —— 只在连续重复达到阈值时注入一句提醒，
  决定权全在模型。默认阈值 `[3, 5, 8]`：第一次是一句短的通用提醒，
  之后每次是详细版（点名工具、连跑次数、规范化后的参数，参数按字符数截断）。
- **键 = (工具名, 规范化参数)**，规范化 = 深度键排序 ＋ 序列化，
  所以只有属性顺序不同的两次调用算同一次。
- 🔴 **被拒绝的调用也计数** —— 检测挂在"执行后"，被闸门拒的调用也会走到；
  模型反复撞一个被拒的调用，正是最该打断的循环。
- 🔴 **被排除的工具是"透明"的：既不加也不重置计数。**
  否则 `查A → 记个账 → 查A` 就把循环洗白了。
- 阈值配置**加载时失败要响**：空列表、非整数、小于 2、重复值 —— 一律抛错，
  绝不静默回落到默认值。
- 只在内存里，会话恢复后计数清零 —— 它是启发式提醒，不是要记账的不变量。

### A.4 `ask_user_question`（`packages/interaction/tool-ask-user`）

- 一次可以问多个问题，每个带**稳定 id**（答案里原样回传）、问题文本、
  可选的短标题、可选的选项列表（`label` ＋ `description`）、多选开关。
- **推荐的选项放第一个，label 后面加 `(Recommended)`。**
- 答案形状固定：`{"answers":[{"id":"...","selected":["<label>"],"custom":"<自由文本>"}]}`。
  `custom` 在多选时是**补充**，在单选时是**覆盖**。
- 这个包**不渲染 UI、不知道输入怎么收集** —— 它只把模型的参数翻译成一个请求，
  再把人的回答交回循环。UI 是另一层的事。

### A.5 「模型眼里长什么样」文档段

每个包的 README 里有一段固定结构的 `Model Experience`：

- **模型看到的原文**（不是描述，是那段文本本身），分场景写；
- **token 影响**（固定成本？数据相关？等人的时候花不花 token？）；
- **KV cache 影响**（是纯追加、还是会打破可复用的请求前缀）。

### A.6 每模块一条运行时不变量

- 每个包必须有一个 `invariant` companion：要么注册一条**真能跑的**检查，
  要么导出一个空的、且注释开头必须是 `No runtime invariant:` ＋ **针对这个包的**理由。
- 一个 `verify` 脚本机械地拒掉：模板生成的痕迹、没解释的空壳、
  写了检查却不用报错器的、注册名不对的。
- 约束边界写得很死：**检查只能断言"事件流或可变数据的关系"，
  不能断言"某个服务或方法存在"** —— 后者是类型系统的事，不是运行时不变量。
- 失败信息带包名：`invariant violated by "<包名>": …`，可归属。

### A.7 每工具超时（`packages/guard/timeout-policy`）

- 预算**由工具自己在定义里声明**（`timeoutMs`），策略插件零配置，只负责执行 ——
  所以不可能写错工具名。
- 超时返回一个**结构化的错误结果**（`code: 'TOOL_TIMEOUT'`），**不是抛异常**。
  我们踩过这个的近亲：MCP 工具错误没分支 → 错误体被当成"找到日志"上报。
- **协作式，不是硬杀**：信号只是通知，真正停下来是工具自己的责任。
  所以**只有会转发信号的工具才该声明超时** —— 声明了却不理信号的工具，超时不会停。

---

## 附录 B —— 这一轮已经确定的事（不用再测）

外网在自己这边核过的，直接用：

- `retriever/code.py` 的 `read_file` / `search_code` **硬绑 `config.MIRROR`**
  （`_resolve_inside_mirror`，`realpath` ＋ 前缀检查）。这是正确的边界，**plan 不动它**。
- `webapp/agent.py` 的 ask 模式循环：`MAX_TOOL_ITERS = 8`；工具入口只有
  `tools.dispatch` / `tools.dispatch_events`；预算对象是 `context_budget.Budget()`。
- `fit_tool_result` **已经是结构感知的截断**（不会给模型半截 JSON）——
  这一半我们做对了，spill 要补的是**另一半：切掉的部分拿不回来**。
- `fit_history` 现在是**丢**，不是总结，但**会告诉模型丢了几轮**、并保留开场那一问；
  会话 store 里存的仍是**完整**对话。这是诚实的降级，不是 bug。
- `config.py` 里 `CONTEXT_LANE_SHARES` 已经给 `compaction` 留了 5% 的车位，
  注释写明"要多花一次模型调用所以推迟"。
- 上游 50 个包里，**sandbox / code-runtime / lsp / fs / shell / subagent-codex / TUI / goal / plan-mode**
  都是编码 agent 的东西，我们的 assistant 不写代码，**这些不在借鉴范围内**，不用看。

## 附录 C —— 与 RUNBOOK-83 的关系

**不冲突，可以并行，但有一处复用：**

- RUNBOOK-83 探针 1（现状对齐）已经在问 `webapp/` 的模块清单和签名。
  **那份输出直接拿来用**，本文探针 1 只补三个 83 没问的点（派发点、预算对象、检查点内容）。
- RUNBOOK-83 还在处理「agent 模式答成『已确认事实：无』」——
  那是**证据归一化**的问题，跟这七件借鉴**没有重叠**。
  83 的结论不会改变这份 plan，这份也不会改变 83。
- 如果人手只够跑一份：**先跑 83**。它在修一个已经在生产上发生的退化，
  这一份是防患。
