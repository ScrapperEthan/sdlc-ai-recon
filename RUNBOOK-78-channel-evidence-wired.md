# RUNBOOK-78 (INTERNAL Codex) — 证据层已接入;还差一个文件,以及四个问题

> 你们 RUNBOOK-77 的回报收到了,质量很高 —— 边界写得清楚、没有越界改代码、而且把"不知道"单独报出来。
> 建议里我采纳了绝大部分,**引擎侧已经建好并推送**。
>
> 这一轮要做三件事:
> **① 验证已接入的东西**(只读,约 15 分钟)
> **② 补一个文件** —— 你们已经有这份数据,只是没导出成机器能读的格式。**导出即生效,外网零改动。**
> **③ 回答四个问题**,都能在盒子上自查。
>
> **回报纪律不变:** 聚合计数,不贴数据行,不贴人名,不贴客户数据,不贴代码内容。

---

## 先决条件

```bash
git pull && python -m pytest tests -q -p no:cacheprovider
```

期望 **1503 passed**(1478 + 25 条新增)。

> 你们上一轮报的那个 `1 failed` 我修了,**但修在了我这边**。
> 原因值得说清楚:那条测试断言的是「**我们**内置的说明字典覆盖了 `config/mcp_tools.json` 里的每个操作」——
> 可那个文件是**你们在本地编辑的**。于是你们正常加一个操作,就会让外网的测试变红,
> 而这**不是你们能修的东西**,回报里也只能写成"与外网 registry 不同步"。
>
> 现在改成检查**最终解析出来的说明**(先读配置里的 `purpose`,没有再回落到内置字典)。
> 所以:**你们加操作时在同一处写一句 `purpose` 就行**,不用等外网发版。
> 如果还是红,信息也变成你们能直接处理的了 —— 那说明确实有一行在界面上会没有说明文字。
>
> **一条会因为对方正常干活而变红的测试,只会训练所有人忽略红灯。**

---

# 一、已经接入了什么(请验证)

| 落地 | 位置 |
| --- | --- |
| 证据 reader(独立模块,**没有**塞进 override) | `retriever/channel_evidence.py` |
| 契约旋钮(**你们可编辑**,字段名/渠道枚举/排序都在这) | `config/channel_evidence.json` |
| refresh 报告 | `index/reports/CHANNEL_EVIDENCE.{md,json}` |
| 影响报告新增「归属 vs 连累」 | `impact_report.py` → `direct_channels` / `affected_channels` / `channel_relations` |
| blast radius 按证据强度分层 | `retriever/blast_radius.py` |

你们建议里被采纳的部分:

- **P0-1 独立 reader**:照做。缺文件/非 UTF-8/JSON 损坏 → 空结果 + 明说,不影响主回答。
- **P0-2 保留关系类型**:照做。关系名就是你们提的那套。
- **P0-3 归属 vs 影响**:照做,`direct_channels` / `affected_channels` 分开返回。
- **P0-4 按强度排序**:照做,而且**低置信的代码线索被降级到消息图之下**,和你们的建议一致。
- **P0-5 重算 unknown**:照做,但**拆成了四桶不是两桶**(见下)。
- **P1-1 多渠道多引用**:reader **三种结构都能读**(v1 / v2 / 扁平列表),所以你们要不要升级 v2 随时可以,**不需要等外网**。
- **P1-2 provenance / freshness**:照做,并且**每次 refresh 会拿引用去镜像里重新核对一遍**,报告有多少条已经对不上(镜像更新后行号会漂)。

**故意没照做的一处**:`note` 字段**默认不返回**。
理由:它是扫代码生成的,可能夹带代码片段、厂商标识、接口地址,而它会一路进模型、上界面。
**引用才是证据,note 只是注释。** 等你们回答了下面第 4 个问题,再决定要不要放开(配置里一个开关)。

---

# 二、还差一个文件:**扫描范围**(这是本轮最重要的一件)

## 为什么必须有

你们交的证据文件回答的是「**找到了什么**」。但一个没有证据的仓库,可能是三种完全不同的情况:

1. **看过了,确实干净** —— 这是一个有价值的结论
2. **看过了,匹配到了,但存不下引用** —— 你们报的那 55 个
3. **压根不在本轮扫描范围** —— 377/460,所以这是最常见的一种

**这三种现在没法区分**,因为证据文件里没有"我看过哪些"。

你们自己在前端建议 P0-6 里专门强调过要把「有意不扫描」和「扫了但不知道」分开 ——
**完全同意,但生成的文件让这件事做不到。**

引擎现在的行为是:**没有这个文件,就拒绝在三者之间做选择**,全部记为
`unknown_scope_unknown`,并在报告里明说"无法区分"。这是故意选的最不好看的那个读法 ——
因为产出的是**要拿去发通知的名单**,一个从没被看过的仓库被报成"没有渠道",就是一个不该漏的团队被漏掉。

## 文件格式(即插即用,导出即生效)

路径:`index/repo_channel_scan_scope.json`(可用环境变量 `SDLC_CHANNEL_SCAN_SCOPE` 改)

```json
{
  "generated_at": "<导出时间>",
  "codegraph_manifest": "<本轮用的 manifest / snapshot 标识>",
  "scanned": ["<本轮实际扫过的仓库名>", "..."],
  "unresolved": [
    {"repo": "<仓库名>", "reason": "<为什么存不下引用>"}
  ]
}
```

- `scanned` 就是那 377 个。你们盒子上已经有反过来的那份
  (`index/reports/RUNBOOK-77-CODEGRAPH-UNCOVERED.txt`),取补集即可。
- `unresolved` 就是那 55 个。**`reason` 很重要** —— 如果是"文件太大 / 是生成代码 / 二进制 / 行号定位不到",
  这些是可能修的;如果不写原因,它就只是一个数字。
  **只给名字不给原因也能读**(会当成空 reason),不要因为凑不齐原因就不给这份清单。
- 字段名如果你们的生成器叫别的,**不用改生成器** —— 改 `config/channel_evidence.json` 里的
  `scope_aliases` 即可,大小写/下划线/连字符都自动忽略。

## 补上之后会发生什么

`unknown` 从一个数字变成四个,每个的处理方式都不一样:

| 桶 | 含义 | 该怎么办 |
| --- | --- | --- |
| `has_direct` | 名字 / 表 / 代码 / 配置 说它自己管这个渠道 | 可以直接用 |
| `relation_only` | 只有传播关系 —— 会被连累,但不知道自己管什么 | 可以说"会受影响",**不能**说"它发这个" |
| `unknown_scanned` | 看过了,没找到 | **这是一个结论** |
| `unknown_unscanned` | 本轮范围外 | **不是缺陷,也绝不能读成"没有渠道"** |

---

# 三、四个问题(都能自查)

### Q1 —— 扫描范围文件(就是上面第二节,做完即可)

### Q2 —— 那 55 条存不下引用的,原因分类是什么?

放进上面的 `unresolved` 即可。请在回报里给**按原因分组的计数**。

### Q3 —— 证据文件的实际结构是什么?

我看了你们的回报,有一处对不上,想确认不是我理解错:
渠道分布表六个渠道加起来正好 **322**,和"生成 322 条证据记录"完全相等。
如果一条记录可以带多个渠道,这两个数应该不相等。

**请给三个数**(不用改文件,`python` 里数一下就行):

1. 顶层 key 有多少个(= 有证据的仓库数)
2. 有多少条记录的 `channels` 长度 > 1
3. 有没有同一个仓库出现两条独立证据的情况

> 问这个**不是要你们改格式** —— reader 三种结构都能读。
> 只是想知道**有没有信息在生成的那一刻就丢了**:v1 结构下一个仓库只有一个 `citation`,
> 那么"这个仓库既有短信证据又有邮件证据"这件事就存不下来。
> 如果确实丢了,升级成 v2 随时可以,引擎那边不用动。

### Q4 —— `note` 里会出现什么内容?

请给 **3 个把真实值换成 `<...>` 的例子**。**不要贴真实内容。**

判断标准很简单:
- 如果是「在某个类里发现渠道关键字」这种**描述性文字** → 风险低,可以放开
- 如果**原样抄了代码行 / 含接口地址 / 含厂商标识** → 必须先过脱敏闸门再放开

---

# 四、跑一次 refresh,把报告里的数字发回来

```bash
python refresh.py
```

然后看 `index/reports/CHANNEL_EVIDENCE.md`,回报这几项:

1. **加载**:records loaded / repos / shape(`v1` 还是 `v2` 还是 `list`)
2. **被拒绝**:`rejected` 多少,分别是什么原因
   —— 期望是 0,如果不是 0,**第一嫌疑人是字段名对不上**,改 `config/channel_evidence.json`
   的 `field_aliases`,不要改生成器
3. **引用复核**:`ok` / `stale` / `unverifiable` 各多少
   - `stale` = 文件还在但行号超出范围(镜像动过)
   - `unverifiable` = 扩展名不在识别列表里,**引擎按"没验过"算,绝不按"验过了"算**。
     如果这个数不小,请告诉我出现了哪些扩展名(只要扩展名,不要路径)
4. **四桶覆盖数**
5. **冲突表**:代码说一个渠道、名字/表说另一个渠道的仓库有多少个
   —— 这一项你们上一轮完全没提,我加的。**它不是错误,是需要人判一次的东西**:
   最可能的解释是名字是历史遗留、代码是现状,但这得仓库的 owner 说了算,
   而**只有把分歧显示出来,才有人会去判**。悄悄取并集等于让一个错标签被一条对证据保护起来。

---

# 五、还没做的(下一轮,不用等你们)

- **前端分层卡片 + 证据抽屉**:你们的前端建议我基本照单全收。
  点击引用打开源码这个功能本来就有,直接复用。
- **提示词按证据层作答**
- **考题**:你们没提,但这条很关键 —— **光靠提示词按不住模型**,
  它一定会把分层重新揉成一句话。这种回弹只有考题能抓住,准备加 4 道。

---

## 一句话

**你们补一个"看过哪些"的文件,四种 unknown 就自动分开了,外网一行代码都不用动。**
其余三个问题是为了确认有没有信息在生成时就丢了,以及 `note` 能不能安全放行。
