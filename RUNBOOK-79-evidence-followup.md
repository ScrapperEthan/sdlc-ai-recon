# RUNBOOK-79 (INTERNAL Codex) — 你们的回报改了三处引擎;还剩两个问题

> 你们 2026-08-06/07 那份回报**很有用**,四个问题全答了,而且**又抓到我两个错**。
> 这一轮很短:**拉代码 → 重跑一次 refresh → 回两个问题**,预计 10 分钟。
>
> **回报纪律不变:** 聚合计数,不贴数据行,不贴人名,不贴代码内容。

---

## 先决条件

```bash
git pull && python -m pytest tests -q -p no:cacheprovider
```

期望 **1507 passed**。

---

# 一、你们改了什么(三处,都已修)

### 1. 🔴 那 55 个,我理解错了 —— 它们是"干净",不是"存不下引用"

RUNBOOK-77 你们写的是「通过代码但无法保存引用: 55」。**我读成了"匹配到了但存不下出处"**,
于是在 RUNBOOK-78 和界面上都写成了「查到了线索但存不下引用 —— 既不是干净也不是未知」。

你们这轮给的 reason 把它说死了,而且 55 个全都一样:

```
no_supported_channel_marker_in_eligible_source_or_config
```

**有合格的源码/配置文件,读了,里面没有渠道标记。** 那就是**扫过了、确实没有** —— 是一个**结论**,
不是第三种谜团。

已修:
- 字段改名 `unresolved_uncitable` → `scanned_without_evidence`
- 引擎**不再解释你们的 reason**,原样带出来并按 reason 分组计数
- 界面文案改成「**扫过、读了源码和配置、没找到渠道标记 —— 这是一个结论,不是未知**」

> 这和前面五轮是同一个形状:**我给你们的字段安了一个含义,而不是问一句。**
> 只不过这次不是名字/结构/格式,是**语义**。

### 2. 🔴 你们那句"补充"是对的,我的报告在少报

> 「范围外实际是 83 个;报告中的 `unknown_unscanned=48` 只是其中"同时没有其他直接/传播线索"的子集,
> 不能把它读成"83 个没有渠道"。」

**完全正确,而且这个方向最危险** —— 一个**少报了"没看过多少"的数字**,却长得像在报告它。

已修:桶和总数**分开报**。
- `scanned_total` / `out_of_scope_total` = **总人口**(377 / 83)
- `unknown_scanned` / `unknown_unscanned` = **桶**,每个都额外要求"没有任何其他线索"(43 / 48)
- 报告和界面都明说后者是前者的**子集**
- `scanned_without_evidence`(55)也标明**与 `unknown_scanned`(43)重叠,不能相加**

### 3. 那 5 条不可核验的引用 —— 是我的检查器缺扩展名

`.js` 3 条、`.groovy` 2 条。**这不是你们的问题,是我的引用检查器不认识这两个扩展名。**

而且这里有个**更早就存在、你们的测量顺手照出来的洞**:扩展名不在列表里的引用
**不会报错,是根本不进检查** —— 报告会显示「0 条引用」而不是「1 条未核验」。
所以助手在回答里引用一个 `.groovy` 构建脚本或 portal 的 `.js`,**引用守卫一直是完全跳过的**。

已修:加了 `groovy` / `js` / `jsx` / `ts` / `tsx`,并加了一个边界判断
(否则 `jsx?` 排在 `json` 前面会把 `package.json` 截成 `package.js`,**把一条真引用变成找不到的文件**)。

**请重跑后确认那 5 条从 `unverifiable` 变成 `ok` 或 `stale`。**

---

# 二、重跑一次

```bash
python refresh.py
```

看 `index/reports/CHANNEL_EVIDENCE.md`,回报:

1. **引用复核**:`ok` / `stale` / `unverifiable` —— 期望 `unverifiable` 掉到 **0**
2. **新的两行**:`in the scan scope` / `outside it by design` —— 应该是 **377 / 83**
3. `in scope but producing no evidence record` 那一行,后面跟的 reason 分组对不对

---

# 三、只剩两个问题

### Q1 ⭐ —— 扫描时**找到过**一个仓库多个渠道吗?(这个问题决定要不要升 v2)

你们说:

> `channels` 长度大于 1: **0**;同一仓库多条独立证据记录: **0**
> 说明当前 v1 文件确实只能保留每仓库一条引用

**但这句话没区分两种情况**,而这两种的结论完全相反:

| 情况 | 意思 | 该怎么办 |
| --- | --- | --- |
| **A. 扫描本来就只找到一个** | v1 没丢东西 | v1 够用,不用动 |
| **B. 扫描找到了多个,写文件时只留了一个** | **信息在落盘那一刻丢了** | 值得升 v2,**引擎已经能读,零改动** |

**怎么自查**:在生成证据的那一步(写文件之前),统计一下
**「同一个仓库命中了 ≥2 个不同渠道」的仓库有多少个**。

- 如果是 **0** → 情况 A,这条就结了,不用升级。
- 如果 **> 0** → 情况 B,请升 v2。格式在 `config/channel_evidence.json` 里,
  **reader 已经支持,你们换格式那天直接生效,外网不发版。**

> 为什么值得问:322 条证据 = 322 个仓库 = 每个仓库正好一个渠道。
> 一个真的既发短信又发邮件的仓库,现在只会被报成发一个 —— **正好是我们花这么大力气在消除的那种少报。**

### Q2 —— 那 11 个冲突,谁来判?

「代码证据与名称/业务声明冲突: 11,已保留供人工判断,没有静默合并」—— 这正是设计意图。

但它现在**停在那里**。请给一个**不含仓库名的形状描述**,好让业主知道要判什么:

- 11 个里,**代码说 A、名字说 B** 的有几个?**代码说 A、业务表说 B** 的有几个?
- 冲突的证据里,**高置信几个、低置信几个**?
- 你们**倾向于**哪一边是对的(代码是现状 / 名字是历史遗留)?——**你们的判断也是数据**,写下来

---

# 四、`note` 的结论:不用开了

你们给的三个脱敏样例都是同一个通用模板(「在 `<文件>` 中发现 `<渠道>` 相关静态扫描据」)。

也就是说 **note 里没有任何超出引用本身的信息** —— 引用已经指到了那一行。
所以**不是"暂时不开",是没有理由开**。`include_notes_by_default=false` 保持不变,
除非以后 note 真的开始带独立信息。

你们那句「即使当前模板风险较低,也应先经过 egress/脱敏门禁再开放」—— 同意,而且更省事:**不开就不需要门禁。**

---

## 一句话

**Q1 决定要不要升 v2(自查一下扫描阶段有没有一仓库多渠道),Q2 把 11 个冲突整理成业主能判的形状。**
其余的我都改完了,重跑一次 refresh 确认那 5 条引用转正就行。
