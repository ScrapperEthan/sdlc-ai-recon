# RUNBOOK-77 (INTERNAL Codex) — 两件**由内网拥有**的活:缩写词典 + 渠道的代码证据

> 这一轮和以前不一样:**不是让你们验证我写的东西,是把两块工作正式交给内网维护。**
> 原因很直接 —— 这两块的数据源只有你们能访问(那个 Excel、那份 mirror、那套 CodeGraph),
> 外网这边**没有能力也没有资格**替你们填值。
>
> **任务 A 完全不需要外网改任何代码**,你们做完就直接生效。
> **任务 B 需要外网加一段读文件的代码**,但格式我在下面先定好了,**两边可以同时开工,互不等待**。
>
> **回报纪律不变:** 聚合计数,不贴数据行,不贴人名,不贴客户数据。

---

## 先决条件

```bash
git pull && python -m pytest tests -q -p no:cacheprovider
```

期望 **1478 passed**。

---

# 任务 A —— 缩写词典(`index/glossary.json`)

## A0 背景:为什么这件事值得做

助手回答问题时,第一步是**看仓库名字判断这个仓库是干什么的**。我们这边的仓库名全是缩写拼起来的,
所以名字看不懂 = 找不准 + 报告读起来像天书。

`index/glossary.json` 是这个项目里**唯一一个没有任何脚本能生成的文件** —— 它在
`retriever/glossary.py` 的文档里就是这么写的。它只在你们的盒子上,外网从来没见过,也不该见到。

**2026-08-05 实测**:token 槽位解码率 78.6%,但**每一段都能解释清楚的仓库名只有 42 / 460**。
第二个数字才是读的人真实的感受 —— 一个名字里只要有一段没解出来,读起来还是天书。

**2026-08-06 业主提供的新信息**:`MDC_Repo_List_Analysis_v0.3.xlsx` 里有第二个页签
**`Keyword in Repo Name`**,列是 `Name in Repository Name` / `Description` / `Sample`,
已经写了大部分常用缩写。

也就是说:**这本词典你们已经写了一大半,只是我们没在用。**

## A1 要做什么

把那个页签转成 `index/glossary.json`。格式是**扁平的一层 JSON**:

```json
{
  "<缩写小写>": "<这个缩写是什么意思>"
}
```

- key **一律小写**(加载时会 `.strip().lower()`,大写也能work,但存小写省事)
- value 就是那一列 `Description` 的文字,中英文都行
- 文件路径 `index/glossary.json`(可用环境变量 `SDLC_GLOSSARY` 改),**已在 .gitignore 里**,
  不会被推走,也不会被外网覆盖

## A2 三条硬规矩(每一条都是真出过事的)

**1. 空的 / `TBC` / `???` / `待确认` —— 不要写进去。**

2026-08-05 现场看到的:盒子上的文件里 `mc`、`api`、`common` 三个词填的是 `TBC ? ???????`,
结果界面上直接渲染成 `mc-hk-hase-api-common (mc=TBC ? ???????, hk=HongKong, …)`。
**读的人分不出这和"已经查清楚了"的区别 —— 不知道被包装成了答案。**

引擎现在会把这类值丢掉(`glossary.is_unfilled()`),所以写进去也不会渲染错;
但**最干净的做法是干脆不写这一行**。你们照片里那张表本身就有空行(比如两三行 Description 是空的),
转换脚本要跳过它们,不要转成空字符串。

> 同样的闸门 2026-08-05 已经复用到枚举加载器上了,原因更严重:在那边一个占位符会让
> `known=True`,直接翻转引擎后面依赖的布尔判断。**"没人告诉我们" 绝不能长得像 "已定义"。**

**2. 必须存成 UTF-8。**

中文 Windows 上用记事本另存为 ANSI/GBK,或者用非 Unicode 控制台重定向写文件,会让这个文件
**整体读不出来** —— 后果是所有 impact 报告里的仓库描述**全部静默消失**(以前是直接崩掉整个报告,
现在会优雅降级成"没有词典",但你会以为词典没生效而去查错方向)。

`GLOSSARY_COVERAGE.md` 会明确告诉你是"文件不存在"还是"文件读不出来",这两种情况的处理方式不同。

**3. 填不出来就留空,不要为了填满而编。**

报告里 `missing` 是一个**正确的状态**,不是失败。编一个像样的解释比留空危险得多。

## A3 该先填哪些 —— 不用自己想

跑一次 `refresh.py`,它会自动生成:

```
index/reports/GLOSSARY_COVERAGE.md
index/reports/GLOSSARY_COVERAGE.json
```

`.md` 里有一张**按"这个缩写出现在多少个真实仓库名里"从多到少排序**的表,三种状态:
`filled` / `placeholder`(有条目但没意义)/ `missing`(压根没有)。

**从上往下填**。排第一的词可能出现在几百个名字里,排最后的可能只出现一次 —— 时间花在前者上。

## A4 请回报这几项(聚合数字即可)

1. **填之前 / 填之后** `GLOSSARY_COVERAGE.md` 顶部那几行,特别是:
   - `token slots decoded: X / Y`
   - `names with EVERY token decoded: N` ← **这个才是真实进度**
   - (`names with at least one token decoded` 请忽略,已实测是饱和指标,填之前就 459/460)
2. Excel 那个页签**一共多少行**,其中**多少行 Description 是空的或 TBC**
3. **表里有、但我们仓库名里根本不出现的词** —— 有多少个。这说明表和实际命名有偏差,值得知道
4. 填完之后**仍然 missing 且排在前 20 的词** —— 这些是 Excel 也没覆盖的,需要人去问

## A5 明确一下归属

**这件事以后就是内网维护的。** Excel 更新了、有人补了新缩写,你们重新跑一次转换 + `refresh.py` 就行,
**不需要通知外网,也不需要外网改代码。** 外网只保证:引擎永远从 `index/glossary.json` 读,
永远丢弃占位符,永远不因为这个文件坏掉而崩。

---

# 任务 B —— 渠道归属的**代码证据**

## B0 背景:现在这块是怎么算的,以及为什么不够

助手回答「这次改动 / 这次故障影响哪些渠道」时,用的"这个仓库属于哪个渠道"这个标签,
目前是这么来的 —— `make_repo_tags.py:132`:

> 拿仓库名去匹配 `sms` / `email` / `push` / `whatsapp` / `wechat` / `letter` 这几个词。
> **名字里有就标上,没有就没标签。**

就这么粗。所以现在的渠道覆盖率约等于"名字里明写了渠道的仓库占比"。

**MDC 那张表补不上这个洞**,而且代码里自己就写着这句话(`enrich_repo_tags.reconcile()` 的
honesty 块):*这张表增加的是业务元数据和交叉验证,不提高渠道归属覆盖率。*
原因是那张表覆盖的是 MDC 名册上那几十个仓库,而**那几十个仓库的名字里本来就写着渠道**;
真正没标签的几百个仓库压根不在表上。

现在已经在算的证据有四种:

| 字段 | 含义 | 强度 |
| --- | --- | --- |
| `channel` | 名字里写着 | 强(它**就是**干这个渠道的) |
| `channel_declared` | MDC 表勾选 | 强(业务方确认) |
| `serves_channels` | 依赖图传播:被某渠道的仓库依赖 | 中(它坏了会**连累**这个渠道) |
| `msg_channels` | 消息图 | 中 |
| `channel_true_dark` | 以上全对不上 | **真的没人知道** |

**缺的是第五种,也是业主点名要的那种:代码里到底有没有真的在发短信 / 发邮件。**

## B1 第一步:先报 CodeGraph 的真实覆盖(**这一步先做,不要跳**)

业主的原话是:*「CodeGraph 未必扫描的完整,但至少已经扫描上的就得加上」* —— 完全同意,
但顺序不能反:**先知道扫到了多少,才知道后面那份证据的天花板在哪。**

请产出一张表 / 一段汇总:

- 一共建了多少个 bundle,成功几个、失败几个
- 每个 bundle 里实际有多少个 repo
- **去重之后,CodeGraph 覆盖了 460 个仓库里的多少个**
- **哪些仓库一个 bundle 都没进**(只要数量 + 一份清单文件留在盒子上,回报只给数量)
- 建失败的 bundle:失败原因是什么(磁盘?超时?语言不支持?)—— 这个可能是外网能帮上忙的

> ⚠️ **扫不到 ≠ 没关系。** 这个项目反复栽的跟头就是"沉默被当成答案":
> 服务端没命中关键词却回了文件尾部,我们标成"命中";0% 被读成"关掉了"。
> **所以没扫到的仓库必须明确标成"未覆盖",绝不能因为"没找到证据"就当成"没有这个渠道"。**

## B2 第二步:找代码级证据

在**已覆盖**的范围内,判断每个仓库到底碰不碰某个渠道。怎么找你们比我清楚,下面只是起点:

- 调用厂商 SDK / 厂商域名的 HTTP 客户端
- 生产或消费的 topic 名里带渠道词
- 配置 key、枚举值、常量里带渠道词
- 类名 / 方法名 / 包名里带渠道词(注意:这条最弱,容易误判,单靠它请标低置信)

**每判断一条,记下是哪一行代码让你这么判断的。** 没有出处的条目引擎会拒绝加载 —— 这是故意的,
和答案必须带引用是同一条纪律。

## B3 第三步:输出证据文件(**不要直接写进 `repo_tags.override.json`**)

`index/repo_tags.override.json` 确实能直接改渠道标签,而且立刻生效 —— 但**请不要用它做这件事**。
原因:写进 override 之后,这个标签就和"名字里写着"变得一模一样,**证据丢了**,
以后没人能回答"我们凭什么说它是短信"。

请写一个**新文件**,`index/repo_channel_evidence.json`,结构如下
(下面全部是占位符,**不是示例值,请不要照抄任何内容**):

```json
{
  "<仓库名>": {
    "channels": ["<sms|email|push|whatsapp|wechat|letter 之一或多个>"],
    "basis": "<code|config|doc|owner>",
    "confidence": "<high|low>",
    "citation": "<仓库名>/<文件路径>:<行号>",
    "note": "<可选,一句话说明>"
  }
}
```

- `basis`:`code` = 代码里找到的;`config` = 配置文件;`doc` = 文档/表格;`owner` = 人告诉的
- `confidence`:只有名字/包名这类弱线索请标 `low`
- **`citation` 必填**,没有出处的条目会被丢弃
- **拿不准的不要写。** 宁可少写,也不要写一条猜的 —— 少写只是覆盖率低,写错是会让人按错误的名单去发通知

## B4 第四步:把"判断不出来的"也交回来

请同时给一份**「看了代码也判断不出来」的仓库清单**(数量 + 盒子上留文件)。

这份清单**和证据一样有价值** —— 它是真实的"不知道",是我们敢对外说"还有 N 个不确定"的依据。
现在那个不确定的数字是虚高的(把其实推得出来的也算进去了),反而让人不敢信整个答案。

## B5 外网这边会做什么(你们不用管)

1. 加载 `index/repo_channel_evidence.json`,校验 citation,丢弃无出处条目
2. 把**证据等级带进答案**:回答"影响短信"时能说清楚是「它自己就是发短信的」还是
   「它坏了会连累短信」还是「业务表里勾了」
3. 把下界的说法从"还有 X 个没标签"改成引用 `channel_true_dark`

**这三条是外网的活,不需要你们等我们,也不需要我们等你们。**

## B6 请回报这几项

1. B1 的覆盖表(bundle 数 / 覆盖仓库数 / 未覆盖数 / 失败原因)
2. `index/repo_channel_evidence.json` 生成了多少条,其中 `basis=code` 多少条、`confidence=low` 多少条
3. B4 那份"判断不出来"的数量
4. 做的过程中**发现我上面哪句话跟你们的实际情况对不上** —— 这一条历来是最值钱的回报

---

## 一句话总结

**任务 A 你们做完就生效,外网零改动。**
**任务 B 你们出证据,外网出读证据的代码,格式已定,同时开工。**

两件事的共同点:**值永远由你们出,外网只出读值的程序。**
过去五轮验证,每一次缺陷都是外网替你们断言了某个东西 —— 先是名字,然后是数据结构,然后是值的格式。
这一轮从一开始就不断言。
