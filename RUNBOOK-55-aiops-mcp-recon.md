# RUNBOOK-55 —— AIOps / MCP 接入侦察(建代码之前先摸清 14 件事)

> **这次要加的能力:** 事故告警来了之后,助手自己判断**该去看哪个应用、哪个文件、哪个时间段、
> 搜什么关键词**的日志,拿到证据,再用现有的代码图/业务图算出**影响了哪些业务、该通知谁**。
>
> **为什么先侦察不直接建:** 这条链路上有一段是**别人的系统**(同事写的三套 MCP + 生产日志),
> 外部完全看不到。看不到就只能猜,而这里猜错的代价比 Round A 那次跨环境错连更大 ——
> 一个是答案错,一个是**把客户数据带进了不该去的地方**。
>
> **谁做:** 内网 Codex 跑;涉及权限和政策的几条(问题 2、13、14)需要项目负责人去问人。
> **回报的是取值、计数、样例结构,不是数据本身** —— 不要贴日志原文,不要贴手机号/邮箱/姓名,
> 不要贴告警原文里的客户标识。需要样例时,**把值替换成 `<masked>` 再贴结构**。
>
> **本 runbook 只读不写。** 不要在盒子上改任何代码,不要动 MCP 那边的任何配置。

已知的三个端点(来自 Confluence `AIOPS` 页截图,未经验证):

```
hkl25254508:8092/sse          LogDream    读日志
hkl25254508:8093/cloudwatch   CloudWatch  AWS 指标/资源/CloudTrail
hkl25254508:8094/mdc_portal   MDC Portal  投递记录/重发判断
```

---

## A 组 · 连不连得上、能不能用

### 问题 1 —— 盒子能不能连上这三个端点?

**怎么看:** 在跑 webapp 的那台机器上,对三个地址各发一次 MCP `initialize` + `tools/list`。

**要回报:**

| 项 | 内容 |
| --- | --- |
| 三个端点各自 | 通 / 不通(不通的话是拒绝连接、超时、还是 404) |
| transport | 是老的 SSE 还是 Streamable HTTP(`initialize` 的响应里能看出来) |
| 认证 | 要不要 token/header/证书?不要就直接说"不要" |
| `tools/list` 返回的**工具名清单** | 只要名字,不要 schema 全文 |
| 典型延迟 | 随便调一个只读工具,记耗时(秒) |

> **判断标准:** 三个都不通 → 这个方向直接停,先解决网络。只有 LogDream 通 → 也能做,
> 先做日志这一半。**不要为了"让它通"去改防火墙或开代理** —— 那是另一件事,要走正规流程。

### 问题 2 ⭐ —— 这三套 MCP 是谁的?我们能用吗?

**这条不是技术问题,是要去问人的。**

**要回报:**
1. 负责人是谁(团队即可,不用人名)
2. 我们的助手去调它们,**需不需要申请**?有没有配额/限流?
3. 他们介不介意我们**把他们的工具包一层**再给我们自己的用户用(这会变成他们的下游依赖)
4. 生产环境的 MCP,我们**只读**的调用有没有额外审批要求

> ⚠️ 这条建议**最先问**。技术全通了但人没点头,等于白做。

### 问题 3 —— 三套 MCP 现在是谁在用、怎么触发的?

**要回报:** 现在是接在 Copilot 里、还是人工手动调、还是接在某个告警流程里?
**同一个告警,现在的人工处理流程是什么样的**(几步、多久、卡在哪)?

> 为什么问:决定我们是**接进他们现有的流程**,还是**并排做一条新的**。前者省事得多。

---

## B 组 ⭐最关键 · 对不对得上号

> 这一组是整件事的成败点。助手要"判断去看什么日志",第一步就是把
> **告警 → 应用 → 日志位置**这条链打通。这三跳里有两跳我们现在完全不知道。

### 问题 4 ⭐⭐ —— LogDream 里的"应用名",和我们的仓库名对得上吗?

**怎么看:** 调 `list_logdream_apps`,拿回它的应用清单。

**要回报:**
1. 一共多少个 app
2. **前 15 个 app 名字原样贴回来**(应用名不算敏感数据)
3. 你的判断:这些名字和我们镜像里的仓库名(`mc-hk-hase-*` / `amet-mdc-*`)是
   **完全一样 / 有规律的变形 / 完全对不上**?
4. 如果是"有规律的变形",规律是什么?(去前缀?加环境后缀?全小写?)
5. 如果"完全对不上",中间有没有第三张表/配置能把两边接起来?

> **判断标准:** 这是本次侦察**最重要的一个答案**。对得上或有规律 → 助手能自己定位日志,
> 整条路走得通。完全对不上又找不到中间表 → 需要人工维护一张对照表,
> 那我会把它做成 `config/logdream_app_map.json`(按 `AGENTS.md`,内网 Codex 的地盘)。

### 问题 5 —— 一个应用底下的日志文件长什么样?

**怎么看:** 挑 **1 个** 和 MDC 投递相关的 app,调 `browse_logdream` 看它的目录。

**要回报:**
1. 目录层级结构(把具体路径里的敏感段替换成 `<masked>`,只要**结构**)
2. 有哪几类日志文件?(`trace.log` / `exception.log` / 还有别的?)
3. 历史文件的命名规律(截图里提到 `exception.log_YYYYMMDD`,确认一下)
4. **一个 app 是不是有多个实例/多台机器各自一份日志?** 如果是,怎么区分?

> 为什么问第 4 点:如果一个应用有 6 个实例,"看日志"就得先决定看哪几个实例,
> 或者六个都看然后合并 —— 这直接改变查询计划的设计。

### 问题 6 ⭐ —— CloudWatch 告警里的资源名,和我们的仓库名怎么对?

截图里出现过这几个真实资源名:

```
preprocRDS_db0CPUUtilizationWARN
preprocECS_mc-hk-hase-batch-letter-postman-job_service_MemoryUsageMINOR
hase-mdc-prod-db-cluster
preprocRDS_dbIFreeableMemoryTooLowMINOR
```

第二个看起来**直接内嵌了仓库名** `mc-hk-hase-batch-letter-postman-job`。

**要回报:**
1. 调 `list_alarms`,把**告警名列表**回报(名字不含客户数据,可以原样贴)
2. 你的判断:资源名里内嵌仓库名,是**普遍规律**还是**个别巧合**?大概多少比例?
3. `get_resource_tags` 能不能拿到 `owner` / `application` / `support group` 这类标签?
   如果能,**标签里的应用名和我们的仓库名对得上吗?**(和问题 4 是同一类问题)

> **判断标准:** 只要"资源名内嵌仓库名"或"tag 里有能对上的应用名"成立其一,
> CloudWatch 告警就能自动落到具体仓库,再接现有的影响图。两条都不成立 → 同样需要对照表。

---

## C 组 · 告警本身

### 问题 7 —— 告警名里的 "Path",是不是 `tbl_use_case_router.delivery_path`?

截图的告警清单里出现了这 8 条路径名:

```
WPB Servicing Realtime High Risk Path      WPB Time Critical Path
WSB CMB Servicing Realtime High Risk Path  WPB Servicing Realtime General Path
WSB CMB Servicing Realtime General Path    WPB Servicing Batch Path
WPB Marketing Batch Path                   WPB Servicing Realtime Express Path
                                           WPB Servicing Realtime Standard Path
```

**要回报:** 这些字符串,在 `tbl_use_case_router.delivery_path` 这一列里**能不能找到**?
匹配率多少?(**这条和 RUNBOOK-54 的问题 3 是同一件事,一起做**)

> **为什么这条价值极高:** 如果对得上,那么**告警文本里就自带了通往业务数据的钥匙** ——
> 不需要任何推断,直接就能算出"这条路上有哪些业务用例、走哪个厂商、该通知谁"。
> 这是唯一一个**不依赖任何 MCP** 也能做出来的能力。

### 问题 8 —— 告警清单能导出吗?

**要回报:** Confluence 那页右边的告警分布(`338: MDC Alert - General SHP API Error` 那一列),
能不能导成 CSV:**告警名 + 条数 + 统计区间**。

> 只要这三列,不含任何事故内容,应该很好拿。用来验证我们的覆盖面。

### 问题 9 —— 一个告警到达时,我们能拿到什么?

**要回报:** 告警是以什么形式到人手上的?
- 邮件?SNOW 工单?企业微信/Teams 消息?webhook?
- **原始载荷长什么样**(把值 `<masked>` 掉,只要字段结构)
- 里面有没有:告警时间、时区、资源名、账号、指标值、告警 ID

> 为什么问:这决定我们助手的**入口**长什么样 —— 是让人粘贴一段文本,还是接一个 webhook。
> 也决定**时区**怎么处理(截图里的证据是 HKT,CloudWatch 内部是 UTC,**这里算错 30 分钟
> 就会查不到日志,然后自信地回答"未发现异常"** —— 这是最容易出的低级错误)。

---

## D 组 · 数据与合规(这一组决定我们敢做到哪一步)

### 问题 10 ⭐ —— 日志里有客户数据吗?

**要回报:**(**不要贴样例,只回答有没有、哪些字段**)
1. `trace.log` / `exception.log` 里会不会出现:手机号、邮箱、客户姓名、账号、身份证号?
2. 有的话,大概以什么形式出现?(字段名?JSON?拼在异常消息里?)
3. 有没有现成的脱敏工具/规则可以复用?

> **判断标准:** 有客户数据 → 原始日志**绝不能进模型上下文、绝不能落盘**,
> 必须在我们这边先做结构化脱敏,只把"异常类名 + 时间 + 计数 + 代码位置"这类
> 无个人信息的结论送进去。这不是可选项。

### 问题 11 —— 事故结论能不能存?

**要回报:**
1. 事故的**结论**(根因一句话 + 影响面 + 处理动作)能不能落盘保存?存在哪?保留多久?
2. 事故的**原始证据**(日志片段)能不能保存?(默认假设:**不能**)
3. 有没有现成的事故记录系统(SNOW?Confluence?)——如果有,我们应该往那里写,而不是自己存一份

> 为什么问:截图里 **338 条是同一个告警**。如果能存"上次这个告警的根因是什么",
> 第 339 次就能从"从头查"变成"上次是 Aurora 人工 roleswap,先看这三个地方"。
> 这是这个场景里价值最高的一件事 —— 但**只存结论,永远不存日志**。

### 问题 12 —— 审计要记什么?

**要回报:** 助手调用生产 MCP,合规上要不要留痕?要留的话记什么?
(我的默认设计:只记 **用户 / 工具名 / 耗时 / 成功失败 / 事故编号哈希**,不记任何返回内容。)

---

## E 组 · 同事已经做好的东西(能复用就别重写)

### 问题 13 ⭐ —— 能不能拿到那些 SKILL.md 的原文?

截图左边那一列是同事写好的排查经验:

```
portal_trace_log_rca/SKILL.md         portal_exception_log_rca/SKILL.md
resend-decision/SKILL.md              email-resend-decision/SKILL.md
sms-resend-decision/SKILL.md          02_alert_handle_flow/README.md
cloudwatch-alert-query/SKILL.md       cloudwatch_ecs_cpu_rca/SKILL.md
cloudwatch_resource_change_rca/SKILL.md
```

**要回报:** 这些文件能不能拿到原文(纯文本,不含数据)。

> **为什么:** 这里面是**真实的排查经验**(先看 trace.log 再看 exception.log、ECS CPU 告警
> 先分 capacity-only 还是 deployment change ...)。这种东西我们从零写只会写得更差。
> 直接复用,而且对同事那边也是"我们在放大你的成果",比"我们也做了一个"好谈。

### 问题 14 —— 有没有已经处理完的事故记录可以当样本?

**要回报:** 能不能拿到 **3~5 个已经查完、有明确结论**的事故,每个包含:
1. 告警原文(客户数据 `<masked>`)
2. 当时查了哪些日志/指标(**关键:查的顺序和理由**)
3. 最终根因

> 截图里那个 Aurora 的例子(`FailoverDBCluster` → 人工 roleswap → 连接池 stale connection)
> 就是完美的样本。**有 3~5 个这样的样本,我在外网就能把"查询计划"写出来并且测试**,
> 不需要连生产。**没有样本,外部这边基本上做不了事。**
>
> 这些样本会存成 `tests/fixtures/aiops/*.json`,所以**必须脱敏**。

---

## 回报格式

```
A1 连通性  : 8092=通/不通 transport=__ 认证=__ 工具数=__ 延迟=__s
             8093=... 8094=...
A2 权限    : 负责人团队=__ 需申请=是/否 配额=__ 对方态度=__
A3 现状    : 现在谁在用=__ 人工流程=__步/__分钟 卡点=__

B4 app名   : 共__个;前15个=[...];与仓库名 一样/有规律/对不上;规律=__;中间表=有/无
B5 日志结构: 层级=__;文件类型=[...];历史命名=__;多实例=是/否,怎么区分=__
B6 资源名  : 告警名清单=[...];内嵌仓库名=普遍/个别,约__%;tag能拿到应用名=能/不能

C7 Path    : 8条在 delivery_path 里能找到__条,匹配率__%
C8 告警CSV : 能/不能导出
C9 入口    : 形式=__;载荷字段=[...];带时区=是/否

D10 客户数据: 有/无;字段=[...];现成脱敏工具=有/无
D11 存结论 : 能/不能;存哪=__;保留=__;原始证据能存=能/不能;现有事故系统=__
D12 审计   : 要求=__

E13 SKILL  : 能/不能拿到原文
E14 样本   : 能拿__个;脱敏后能不能带出内网=能/不能
```

**不要回报:** 日志原文、告警原文里的客户标识、手机号/邮箱/姓名/账号、任何数据行、
MCP 工具的完整 schema。

---

## 拿到答案之后我会做什么(让你知道这次侦察换来什么)

1. **建"查询计划"生成器(外网侧,纯离线可测)** —— 告警文本 → 一个 JSON:
   看哪个应用、哪个文件、哪个时间窗、搜哪些关键词。关键词由我们的代码图和业务数据推出来
   (该路径上的 use case、topic、厂商、这个仓库真实会抛的异常类),
   **这是通用 AIOps 推不出来的部分,也是我们唯一不可替代的地方。**
2. **建 MCP 客户端 + 白名单**(`webapp/mcp_client.py` / `mcp_registry.py`)——
   只调白名单内的工具;`open_portal_login` 这类会产生真实动作的**永远不做成模型可调工具**。
3. **建"事故调查员" sub agent** —— 它碰 MCP、原始日志只在内存里,
   **只把脱敏后的结构化证据包交回主 agent**。主 agent 从头到尾看不到一行原始日志。
4. **接回现有影响图** —— 证据包 → 受影响的业务用例 / 渠道 / 厂商 / 该通知谁 / 代码行,每条带出处。
5. **上下文与工具治理** —— 按模式发工具(现有问答 13 个不变,事故模式另一套),
   给会话历史加预算上限(现在 `session_store.history_for_agent` 没有任何上限,是个隐患)。

**边界承诺:** 全部走一个默认关闭的开关(同 `SDLC_LLM_TOKEN_MODE` 的做法)。
开关关着时新代码路径不可达,**现有功能一行不改、现有测试逐字节通过** —— 这是验收标准,不是口头承诺。

**按 `AGENTS.md` 的分工:** MCP 地址/凭证、应用名对照表、脱敏规则 = 内网(配置文件 + 环境变量,
真实值永不出内网);查询计划、调查员、影响图、prompt = 外部。
